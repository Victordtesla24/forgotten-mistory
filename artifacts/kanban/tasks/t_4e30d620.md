# t_4e30d620 — Scene 3 — #about flagship: GLSL compass field behind the SVG rose (R2, M1)

**Status:** todo · **Priority:** 90 · **Parents:** t_cba10f82 · **Created:** 2026-09-05T06:20:05.604Z

## YOUR ROLE
analyst-programmer — coding (docs/prompt.md §5). SPEC-v10.md §R2 / c16: promote #about from SVG-only (0 canvases, 138 paths) to an R2-grade scene mounted through components/gl/Scene.tsx — a shader field that turns with the compass (cycle-12 scroll-drive semantics kept), no new npm dependency, no new network asset; reduced motion → zero canvases and the SVG stays. Files: components/sections/About/AboutField.tsx, field.glsl.ts, About.tsx, Compass.tsx, About.module.css; tests/overhaul/scene-about.spec.ts (TDD first).

## PROJECT ROOT
/root/forgotten-mistory (VPS srv1356245). Evidence: docs/delivery/evidence/v10-20260905T0515Z/. Live: https://forgotten-mistory.web.app. Static servers already bound by other tenants: :5599 and :8080 — never reuse them; council batteries use :5601 / :5602.

## MANDATORY
Call kanban_complete() when ALL gates pass — i.e. return structured output {task_id, gates:{...}, files_changed:[...], evidence:[...], goal_complete:true}; the orchestrator writes it to the board. Never self-approve; never weaken a check to pass it; every claim cites a command or a path. No secrets in output — read keys by name from /root/.claude/.env.production with a `grep -E '^[A-Z][A-Z0-9_]*='` reader, never `source` it, never print values.

## EXECUTION ORDER
- S-1 Read SPEC-v10.md §R2 + c16 acceptance; read components/gl/Scene.tsx, useGLCapability.ts and components/sections/Experience/CareerStrata.tsx (pattern: one quad, ≤3 noise lookups/pixel, DPR cap, context-loss handling).
- S-2 TDD tests/overhaul/scene-about.spec.ts: at ?gl=force after #about enters view exactly one canvas in #about; reducedMotion:reduce → zero canvases and the 138-path SVG visible; no-GL (getContext stubbed null) fully readable; canvas aria-hidden and behind the content (elementFromPoint over the heading is not the canvas). Run → red.
- S-3 Implement field.glsl.ts (monochrome, no gold; rotation uniform bound to the active compass index / scroll progress) + AboutField.tsx via <Scene>; colours only from lib/palette.ts.
- S-4 Battery; screenshots 1440/1280/834/390 + reduced-motion; commit `feat(about): the compass turns over a field of light` + evidence commit; push the branch.

## QUALITY GATES
- [ ] scene-about.spec.ts red → green
- [ ] tsc/lint/audit 10/10/build green; no new network asset
- [ ] reduced-motion + no-GL paths proven
- [ ] monochrome suite green

## VERIFICATION
```bash
PLAYWRIGHT_BASE_URL=http://127.0.0.1:5601 npx playwright test tests/overhaul/scene-about.spec.ts tests/e2e/about.spec.ts tests/monochrome
```

## HIERARCHY
role_matrix: coding → level 2 → effort **xhigh** (effort_cascade.yaml; depth_cap 4). Model: claude-opus · Max OAuth. max_runtime_seconds 1800 (O1) · goal_max_turns 20.

## PROVIDER
Anthropic via OAuth (CLAUDE_CODE_OAUTH_TOKEN / claude-cli Max session). Never ANTHROPIC_API_KEY.

## STATUS (2026-09-05T06:21:18.415Z)
running — Workflow lane c16 about GLSL (analyst-programmer opus xhigh → reviewer opus max), port 5601

## STATUS (2026-09-05T07:05:13.299Z)
blocked — V-c16 FAIL only because no Scene can mount on main (P100); the field itself passed tsc/lint/audit/build, monochrome, reduced-motion, no-GL and bundle delta 0 kB — re-verify after t_r19r3f9 lands (parent added)
