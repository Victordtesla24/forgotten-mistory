# t_x1_03 — G-H2b — hero scrim becomes a graded text plate, not a 0.86 full-frame wash

**Status:** todo · **Priority:** 97 · **Parents:** t_x1_02 · **Created:** 2026-09-05T12:24:51.635Z

## YOUR ROLE
analyst-programmer — coding (docs/prompt.md §5). From docs/architecture/SIGNATURE-SCENES-TASKS.json (solutions-architect t_g_h2, commit 0b0bf02) — read docs/architecture/SIGNATURE-SCENES-v1.md first. Hero.module.css:157 and :171 lay rgb(10 10 10 / 0.86) across the whole frame, which is why the reviewer called the atmosphere a shy backdrop. Bind the darkening to the reading column: ~0.72 under the type, <= 0.25 outside it, so the light crosses the frame and the copy still clears AA.

## PROJECT ROOT
/root/forgotten-mistory (VPS srv1356245). Evidence: docs/delivery/evidence/v10-20260905T0515Z/. Live: https://forgotten-mistory.web.app. Static servers already bound by other tenants: :5599 and :8080 — never reuse them; council batteries use :5601 / :5602.

## MANDATORY
Call kanban_complete() when ALL gates pass — i.e. return structured output {task_id, gates:{...}, files_changed:[...], evidence:[...], goal_complete:true}; the orchestrator writes it to the board. Never self-approve; never weaken a check to pass it; every claim cites a command or a path. No secrets in output — read keys by name from /root/.claude/.env.production with a `grep -E '^[A-Z][A-Z0-9_]*='` reader, never `source` it, never print values.

## EXECUTION ORDER
- Add TC-HERO-SCRIM-01 to tests/overhaul/hero-first-paint.spec.ts: the hero slot's outer thirds (x<22% and x>78%) are >= 0.06 luminance brighter than the centre reading band
- Capture red: 02-tests-failing.log
- Rework the .stage::after gradient in Hero.module.css from a flat 90deg wash to a column-bound grade
- Capture green plus tests/a11y/text-contrast.spec.ts green in the same run
- Screenshot the hero at 1440, 1280, 834 and 390 CSS px and attach

## QUALITY GATES
- tests/a11y/text-contrast.spec.ts green — brightening until type fails AA is a different bug, not a fix
- flagship-visibility hero COVERAGE >= 0.15, PEAK >= 0.35, MOTION >= 0.004 still hold
- No raw hex added outside app/globals.css and lib/palette.ts (TC-NFR-MONO)

## VERIFICATION
```bash
node scripts/validate/overhaul_static_audit.mjs
PLAYWRIGHT_BASE_URL=http://127.0.0.1:5603 npx playwright test tests/overhaul/hero-first-paint.spec.ts tests/overhaul/flagship-visibility.spec.ts tests/a11y/text-contrast.spec.ts
```

## HIERARCHY
role_matrix: coding → level 2 → effort **xhigh** (effort_cascade.yaml; depth_cap 4). Model: claude-opus · Max OAuth. max_runtime_seconds 1800 (O1) · goal_max_turns 20.

## PROVIDER
Anthropic via OAuth (CLAUDE_CODE_OAUTH_TOKEN / claude-cli Max session). Never ANTHROPIC_API_KEY.

## STATUS (2026-09-05T13:55:19.386Z)
running — dispatched 13:55Z — analyst-programmer xhigh, isolated worktree, port 5620; measure-first: flagship-C + H1c already reworked the scrim (desktop gradient 0.88→0.45→transparent by 66%, phone plates) — only what TC-HERO-SCRIM-01 still fails gets changed

## COMMENT (2026-09-05T14:08:08.217Z)
PUSHED + consolidated 9e41474 'fix(hero): the scrim is a column-bound grade — the light crosses the frame (G-H2b)' + merge 1d25b7a (14:07Z) — TC-HERO-SCRIM-01 was red on main, so .stage::after was reworked; deploying. Awaiting the lane's structured result and a reviewer probe (folded into the G-H2 poster/no-JS review).
