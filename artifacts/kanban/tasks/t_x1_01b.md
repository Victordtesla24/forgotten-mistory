# t_x1_01b — R2 — cut the fragment cost of the four live GLSL scenes (hero 366 ms, about 333, strata 183, skills 67 ms/frame on SwiftShader) without lowering the flagship floors: half-resolution render targets, fewer taps/octaves, early-outs; prove ≥ 3× on the harness

**Status:** todo · **Priority:** 88 · **Parents:** t_x1_01 · **Created:** 2026-09-05T14:17:46.711Z

## YOUR ROLE
analyst-programmer — coding (docs/prompt.md §5). tests/perf/scene-framerate.spec.ts (t_x1_01) measured on SwiftShader (software-rasteriser, 4 cores): hero-atmosphere median 366.6 ms @1440 / 308 @390, about-field 333 / 333, career-strata 183 / 167, skills-bench 66.7 / 33.4 — budgets 16.7 / 20.0. A software rasteriser cannot prove 60 fps on a GPU (D9), but the RELATIVE cost is real and a 2021 phone GPU is not 20× a desktop GPU: the hero and about shaders are doing far more per-pixel work than the picture needs. Reduce fragment cost while keeping every flagship-visibility floor (coverage ≥ 0.15, peak ≥ 0.35, motion ≥ 0.004, still lit) and text-contrast green: render the atmosphere/field quads at half (or third) resolution into an offscreen target and upscale (or use a lower DPR cap per scene via Scene/GLCanvas props), cut noise octaves / sample taps, precompute what does not change per frame, early-out outside the lit region. Acceptance: harness median improves ≥ 3× for hero and about and ≥ 2× for strata (same host, same load band recorded), floors and AA unchanged; visual diff of the 4 scenes at 1440 within tolerance (screenshots before/after side by side, inspected). Record numbers honestly; do not claim 60 fps.

## PROJECT ROOT
/root/forgotten-mistory (VPS srv1356245). Evidence: docs/delivery/evidence/v10-20260905T0515Z/. Live: https://forgotten-mistory.web.app. Static servers already bound by other tenants: :5599 and :8080 — never reuse them; council batteries use :5601 / :5602.

## MANDATORY
Call kanban_complete() when ALL gates pass — i.e. return structured output {task_id, gates:{...}, files_changed:[...], evidence:[...], goal_complete:true}; the orchestrator writes it to the board. Never self-approve; never weaken a check to pass it; every claim cites a command or a path. No secrets in output — read keys by name from /root/.claude/.env.production with a `grep -E '^[A-Z][A-Z0-9_]*='` reader, never `source` it, never print values.

## EXECUTION ORDER
- S-1 Read the four shaders + their field components + components/gl/GLCanvas.tsx (dpr cap) + Scene.tsx; run the harness for a baseline in your worktree (record host load).
- S-2 TDD: the harness is the test — capture 02-baseline.log; add a per-scene cost budget assertion ONLY as a ratio guard (e.g. new median ≤ baseline/3 read from the committed baseline JSON) — never lower the 16.7/20 thresholds.
- S-3 Implement per scene (smallest change first: DPR/resolution, then taps/octaves); re-run flagship-visibility + text-contrast + scene-* specs after each scene.
- S-4 PUSH RULE: tsc + lint + build:static + audit 10/10 → ledger → commit → push (merge origin/main first); battery + harness + screenshots in a follow-up commit.

## QUALITY GATES
- Harness median ≥ 3× better for hero + about, ≥ 2× for strata (same host, load recorded); skills not worse
- flagship-visibility all scenes green at 1440/390; text-contrast both paths green; reduced-motion still lit
- No new dependency; palette via lib/palette.ts; audit 10/10; ledger; pushed

## VERIFICATION
```bash
SCENE_FPS_ARTEFACT_DIR=docs/delivery/evidence/v10-20260905T0515Z/G-X1-01b PLAYWRIGHT_BASE_URL=http://127.0.0.1:5625 npx playwright test tests/perf/scene-framerate.spec.ts tests/overhaul/flagship-visibility.spec.ts --workers=1
```

## HIERARCHY
role_matrix: coding → level 2 → effort **xhigh** (effort_cascade.yaml; depth_cap 4). Model: claude-opus · Max OAuth. max_runtime_seconds 1800 (O1) · goal_max_turns 20.

## PROVIDER
Anthropic via OAuth (CLAUDE_CODE_OAUTH_TOKEN / claude-cli Max session). Never ANTHROPIC_API_KEY.

## STATUS (2026-09-05T14:38:01.219Z)
running — dispatched 14:38Z — analyst-programmer xhigh, isolated worktree, port 5625; cap ≤2 Chrome-heavy (S5/S6 battery + this)

## COMMENT (2026-09-05T14:54:34.202Z)
PUSHED af7355a (14:53Z, consolidating): Scene/GLCanvas gain resolutionScale (default 1; 0.5 floor, 1.75 ceiling unchanged); hero, about, strata render at 0.5 (quarter of the fragments, upscaled) — no shader source edited so the 4K poster stays faithful; skills untouched (hairline graticule, not the driver). Baseline in-worktree: hero 227 ns/px, strata 96, bench 162 (fill-bound); About field 1866 ns/px at 384×384 — attribution probe: ~60% of its frame is the page's own composite at #about and ~40% canvas compositing, so 3× is NOT reachable from field.glsl.ts or its resolution → child task t_x1_01d. tsc/lint/build/audit clean. After-harness numbers arrive with the lane result; per-Deploy reviewer will re-measure floors + AA.

## COMPLETE (2026-09-05T15:10:13.886Z)
LANE RESULT (af7355a + b0e038b evidence, live 1122ca1b+): resolutionScale prop; hero/about/strata at 0.5. Harness (software-rasteriser, load 7–14 recorded): hero 433→100 ms @1440 (4.3×), 433→67 @390 (6.5×); about 275→100 @1440 (2.75× — MISS: ~60% of that frame is the page composite at #about → child t_x1_01d), 492→67 @390 (7.4×); strata 67→50 @1440 (1.3× vs lane baseline; 3.7× vs the committed t_x1_01 baseline — inside the host's noise band, which moved the untouched skills control 3.5×), 183→33 @390 (5.5×); skills unchanged by design (hairline graticule). Floors hold on all four (coverage 26–100%, peak .42–.83, motion .007–.038), TC-CONTRAST-01/02 both widths green, first-paint + scrim specs green, poster byte-identical. Thresholds 16.7/20 untouched (still red — no 60 fps claim). NOTE: its worktree battery showed vitrine-field and listen-field failing floors (8 cases) — the live reviewer found listen PASS and vitrine FAIL only at 390; margins are thin (listen motion 1.07×) → watch in the next probes. Honest goal_complete=false on the about@1440 clause; closed here as delivered-with-child.
