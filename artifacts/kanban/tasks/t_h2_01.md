# t_h2_01 — SPD instrument — luminance-weighted plane dominance, baseline red printed

**Status:** todo · **Priority:** 96 · **Parents:** — · **Created:** 2026-09-05T15:19:23.291Z

## YOUR ROLE
tester — testing / qa (docs/prompt.md §5). [HERO-TASKS.json g2h1-01-spd-instrument] HERO-FOLD-v2 §3 turns 'the plane dominates' into a number. Author scripts/validate/hero_plane_dominance.mjs and tests/overhaul/hero-plane-dominance.spec.ts. Capture the fold at dsf 1; L = WCAG relative luminance (reuse the helper in tests/overhaul/flagship-visibility.spec.ts); ground G = 10th-percentile L of the fold; light mass m = max(0, L - G). Ink set I = union of fold text-leaf rects + media rects (img/video/svg) + every element whose computed background-color alpha >= 0.5, each dilated 8px, read from the live DOM. Plane P = fold minus I. TC-HERO-PLANE-01 asserts SPD = sum_P(m)/sum_fold(m) >= 0.75; TC-HERO-PLANE-02 asserts the PLANE-2 floor sum_fold(m)/(W*H) >= 0.045 so a black fold cannot score 1.0. Run at 1440x900, 1280x800, 834x1194, 390x844 on BOTH /?gl=force and the reduced-motion still. Print G, sum_fold m, sum_P m, SPD and the excluded rect list in every assertion message so a reviewer re-derives rather than trusts. Record the red baseline; change no production file. Runs in parallel with g2h1-02 — this lane is the gate, that lane is the first visible pixel.

## PROJECT ROOT
/root/forgotten-mistory (VPS srv1356245). Evidence: docs/delivery/evidence/v10-20260905T0515Z/. Live: https://forgotten-mistory.web.app. Static servers already bound by other tenants: :5599 and :8080 — never reuse them; council batteries use :5601 / :5602.

## MANDATORY
Call kanban_complete() when ALL gates pass — i.e. return structured output {task_id, gates:{...}, files_changed:[...], evidence:[...], goal_complete:true}; the orchestrator writes it to the board. Never self-approve; never weaken a check to pass it; every claim cites a command or a path. No secrets in output — read keys by name from /root/.claude/.env.production with a `grep -E '^[A-Z][A-Z0-9_]*='` reader, never `source` it, never print values.

## EXECUTION ORDER
- Read tests/overhaul/flagship-visibility.spec.ts for the luminance helper, the isolated-slot capture and the ?gl=force + settle pattern
- Read tests/e2e/hero-fold.spec.ts for FOLD/STAGE/PORTRAIT_IMG selectors and the foldArea helper
- Author scripts/validate/hero_plane_dominance.mjs as the shared measurement (importable by the spec, runnable standalone with --base)
- Author tests/overhaul/hero-plane-dominance.spec.ts with TC-HERO-PLANE-01 and TC-HERO-PLANE-02 parameterised over 4 widths x 2 paths
- Capture the baseline: docs/delivery/evidence/v10-20260905T0515Z/G2H1/01/02-tests-failing.log with the real SPD per width per path
- Confirm the DOM rect exclusion list is printed, not summarised — a reviewer must be able to reproduce I

## QUALITY GATES
- Thresholds are exactly 0.75 (SPD) and 0.045 (PLANE-2); lowering either to make a run green is a violation
- Ground G is measured from the capture, never assumed from a declared background-color
- Both paths measured — a GPU-only number is not evidence for the no-GL reader
- Zero production files touched; git diff --name-only shows only scripts/validate, tests/ and docs/delivery/evidence

## VERIFICATION
```bash
npm run build:static
python3 -m http.server 5635 --directory out & sleep 3
PLAYWRIGHT_BASE_URL=http://127.0.0.1:5635 npx playwright test tests/overhaul/hero-plane-dominance.spec.ts
node scripts/validate/hero_plane_dominance.mjs --base http://127.0.0.1:5635
```

## HIERARCHY
role_matrix: testing / qa → level 2 → effort **xhigh** (effort_cascade.yaml; depth_cap 4). Model: claude-opus · Max OAuth. max_runtime_seconds 1800 (O1) · goal_max_turns 20.

## PROVIDER
Anthropic via OAuth (CLAUDE_CODE_OAUTH_TOKEN / claude-cli Max session). Never ANTHROPIC_API_KEY.

## STATUS (2026-09-05T15:20:06.228Z)
running — 15:19Z dispatched in parallel with t_h2_02 — tester xhigh, SPD instrument, baseline red printed, no production file changed

## COMMENT (2026-09-05T16:57:53.011Z)
1556Z: SPD instrument may still be useful for G-H1; reclaim if swarm dead. Do not block visible fold APs.

## STATUS (2026-09-05T16:57:53.057Z)
ready — 1556Z reclaim — swarm stopped
