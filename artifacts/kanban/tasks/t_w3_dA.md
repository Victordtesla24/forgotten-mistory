# t_w3_dA — WAVE-3 DIRECTION A (solutions-architect, max) — the title-sequence school: the page as a monochrome opening title sequence; the name written by light through volumetric letterforms, dust and shafts with depth, anamorphic streaks, grain, camera dolly and rack focus on scroll, cinematic cuts between sections; every visualisation rebuilt as an instrument in the same light; removal register, per-section spec at four viewports, instrumented CQ gates + AQ rubric, TDD cases, ≤30-min hero-first slices — docs/architecture/CINEMATIC-VFX-v1-A.md

**Status:** ready · **Priority:** 100 · **Parents:** t_w3_own · **Created:** 2026-09-06T05:39:52.589Z

## YOUR ROLE
solutions-architect — architecture / requirements_analysis (docs/prompt.md §5). Design ONE complete direction for replacing the whole visual layer, independently of the other direction (do not read CINEMATIC-VFX-v1-B.md if it exists). The Owner's words: 'remove all this shabby UI/UX, visualisations etc., and replace it with MARVEL STUDIOS grade visual effects and visualisations quality and sophistication. Only black, white..' IMMOVABLE (docs/prompt.md + CLAUDE.md + the Owner): (1) every effect is black, white and grey — chroma 0; gold (--gold) stays exclusively the sourced-claim mark (caliper jaws, measured-in-production mark, live repo URLs) and never appears in any effect (Owner: 'Only black, white..'; PM assumption logged in t_w3_own); (2) every word on the page stays the content files' words (app/data/portfolio/*.ts) — the site never claims 'Marvel', never uses Marvel/Disney IP, imagery, logos, marks or music; named sequences are study material for craft only (R7 zero fabrication); (3) LCP < 2.5 s, CLS < 0.05, no shipped asset > 500 kB (critical-path video ≤ 2.5 MB, assets/avatar/** on demand ≤ 5 MB), nothing plays by default, no audio autoplay; (4) reduced-motion path = a static composition that still passes every static gate; no-GL path (?gl=off) fully readable — the scene is never the content; (5) keyboard-navigable, contrast ≥ 4.5:1 for every glyph over any plane; (6) G-MV1: Ask Mini Vic never hidden below 834 px; TC-MV-CLICK-01 first-fold click; (7) DPR capped in every scene; the bar is 60 fps on a mid GPU at 1440 (SwiftShader ?gl=force is a smoke test, never an fps claim); (8) six sections in this order with these ids: #hero #about #experience #skills #vitrine #listen, plus MiniVic; (9) every test contract may be superseded consciously but each superseded test is REPLACED by a stronger one in the same slice, never deleted silently; (10) O5: slices ship visibly ≤ 30 min each, hero first, and the removal of an old element happens inside the slice that replaces it (no bare removal, no regression window); (11) stack: Next 15 static export, React 19, three 0.165, @react-three/fiber 9.7.0, drei 10.7.8, framer-motion 11, sharp; GSAP/ScrollTrigger, postprocessing, troika-three-text are NOT installed — you may specify adding them (pinned, MIT/permitted licences) with the bundle cost stated; the 18c6beb page-wide GPU crash (R3F 8 vs React 19) is the cautionary tale — every GPU path gets a ?gl=force 0-pageerror probe. Host: VPS srv1356245, 4 cores, ~4 GB RAM free, no GPU — design for the visitor's GPU, verify structure on SwiftShader.

## PROJECT ROOT
/root/forgotten-mistory (VPS srv1356245). Evidence: docs/delivery/evidence/v10-20260905T0515Z/. Live: https://forgotten-mistory.web.app. Static servers already bound by other tenants: :5599 and :8080 — never reuse them; council batteries use :5601 / :5602.

## MANDATORY
Call kanban_complete() when ALL gates pass — i.e. return structured output {task_id, gates:{...}, files_changed:[...], evidence:[...], goal_complete:true}; the orchestrator writes it to the board. Never self-approve; never weaken a check to pass it; every claim cites a command or a path. No secrets in output — read keys by name from /root/.claude/.env.production with a `grep -E '^[A-Z][A-Z0-9_]*='` reader, never `source` it, never print values.

## EXECUTION ORDER
- S-1 Read in this order: artifacts/kanban/INBOX/OWNER-DIRECTIVE-20260906T0529Z.md and the four Owner screenshots in docs/delivery/evidence/v10-20260905T0515Z/OWNER-20260906T0529Z/ (Read renders PNGs — look at them; they are the FAIL baseline); docs/prompt.md in full (§0.1, §0.3, §2.1 stack, R1–R12, §14); CLAUDE.md prime directives; docs/adversarial/GAP-BACKLOG.md; docs/delivery/evidence/v10-20260905T0515Z/G-REV/fda84067/08-adversarial-review.md (rev7: what a reviewer measured on the current hero); docs/architecture/{HERO-SETPIECE-v3.md,SIGNATURE-SCENES-v2.md,ABOUT-STORY-v2.md,LISTEN-FLAGSHIP.md,ASSET-LADDER.md} (what exists — all of it is candidate for replacement; note which INSTRUMENTS must carry: SPD hero_plane_dominance ≥ 0.78, the About story metric window, palette chroma, first-fold click); every file under components/sections/*/ (tsx, glsl.ts, module.css), components/gl/{Scene.tsx,useGLCapability.ts}, lib/palette.ts, app/globals.css, app/layout.tsx, app/page.tsx, app/data/portfolio/*.ts (the words — read-only for you), scripts/validate/{hero_plane_dominance.mjs,overhaul_static_audit.mjs}, package.json, tests/ inventory (ls -R tests | head -200) and the specs that touch scenes/hero/about/experience/vitrine/skills/listen.
- S-2 Removal register (docs table): every current visual element per section — id/file, what it is, the concrete reason it fails the Owner's bar as seen in the screenshots (state what you see: flat black plates on a grey blur, a static compass ring, grey bars on a striped field, line-drawn diagrams in boxes… — observations, not adjectives), and the exact replacement. Nothing is 'polished'; every row is replaced.
- S-3 DIRECTION A — the title-sequence school: the page is a monochrome opening title sequence. Light is the material: the name is WRITTEN BY LIGHT (SDF/MSDF letterforms with volume; a light sweep passes through and behind the glyphs; the figure is lit by the same source), volumetric shafts and dust with real depth, anamorphic white streaks and halation, film grain and edge darkness, a camera that dollies and racks focus as the reader scrolls, section changes as cinematic cuts/wipes/light-leaks (monochrome). Visualisations become instruments in the same light: About = the ten dimensions as an aperture/orrery of ten blades in light (7 answered blades closed and lit, 3 open blades held open and dark — the story metric carries), Experience = sixteen years as a lit volume to scale (bars as light shafts whose length is the duration; sourced figures carry the gold mark, open brackets stay dark), Skills = the calibration card as a lit bench with the untested cells unlit, Vitrine = six cards as depth-stacked frames with rack focus, Listen = the closing card. Specify the type technique (MSDF via troika-three-text or drei Text vs CSS+SVG mask with a WebGL light pass behind — pick, with bundle cost), the light model, the post stack and its cost, and how the no-GL still is produced (pre-rendered AVIF frame of the same light, ≤ 500 kB).
- S-4 Per-section spec (a table + prose per section, six sections + MiniVic chrome): storyboard beats (enter / rest / scroll-through / exit, with timings), geometry at 1440x900 · 1280x800 · 834x1194 · 390x844 (plane bounds, figure bounds, text boxes — numbers), render technique (pass list: geometry/instancing, raymarch, post passes, text technique, DPR cap), the visualisation's DATA CONTRACT (which content-file fields drive it — the About ten dimensions with 7 answered / 3 open; the eight roles to scale with three sourced figures and five open brackets; the six repositories with limits; the calibration card's tested/untested split) so the cinematic instrument still tells the section's story honestly, asset list with sizes (≤ 500 kB each, total stated), perf budget (ms per pass), degradation ladder (discrete GPU → integrated → ?gl=off still → reduced-motion), a11y (contrast, focus order, alt), the recruiter sentence the section is designed to produce.
- S-5 CINEMATIC GATE SET — turn 'Marvel Studios grade' into instruments (CQ-01…; each with a measurement method and threshold at 1440x900 and 390x844): e.g. luma dynamic range in the first fold (p99 − p1 of the rendered frame ≥ 0.85), flat-fill ratio (share of viewport pixels in any 64x64 tile with luma stddev < 0.01, excluding text boxes) ≤ 0.15, depth layers with distinct parallax ratios ≥ 3 (instrumented via window.__fm hooks exposing layer transforms), light-through-type (the name's glyph luma varies ≥ 0.35 across a 3 s sweep — temporal instrument via render(t)), volumetric term present (a raymarched or layered density with visible scattering gradient ≥ 4 stops across the plane), grain amplitude in band (σ 0.01–0.03 luma), transitions (no section boundary reads as a hard edge: the luma discontinuity across the boundary at every scroll position ≤ 0.2 unless a deliberate wipe is in flight), frame time p95 ≤ 16.7 ms at 1440 DPR 1 on a mid GPU (recorded with the renderer label; SwiftShader numbers are logged as structural only), reduced-motion still passes every static CQ, chroma = 0 outside gold marks, SPD ≥ 0.78, LCP/CLS unchanged. Plus an AESTHETIC RUBRIC (AQ-01…AQ-08, scored 0–5 from screenshots by an independent reviewer): light as material, depth and atmosphere, typography struck into the image, motion with intent (enter/rest/exit beats), composition (single dominant plane), material finish (grain/halation/edge darkness), restraint (no ornament that carries no meaning), coherence across the six sections. Name reference frames for study only (e.g. the Marvel Studios 2016 fanfare/logo sequence — light through letterforms and page-flip volumes; Perception's Thor: Ragnarok and Iron Man 3 title sequences; Kyle Cooper's Se7en titles; the Loki TVA sequences) — describe the CRAFT observed, never copy assets, never reference them in site copy.
- S-6 TDD cases FIRST (file · assertion · threshold · viewport): one per CQ, the carried instruments (hero_plane_dominance ≥ 0.78 at 4 viewports × gl/no-gl; About story metric window; palette chroma; first-fold click at 390; G-MV1; keyboard path; ?gl=force 0 pageerrors and canvases ≥ 1; ?gl=off readability; reduced-motion still; LCP < 2.5 s; CLS < 0.05; asset budgets; text-contrast), and the instruments the AP must expose (window.__fm hooks: render(t), layers, grain σ, renderer label).
- S-7 Slices: ≤ 30-min analyst-programmer tasks (10–16), hero first, each independently shippable and VISIBLE on the live URL, in dependency order; for each: id, title, files (exact), what old element it replaces (from S-2), tests it turns green (from S-6), the visible result a reviewer can see, minutes. The first slice must be a visible hero change shippable in ≤ 30 min. S-8 Risk register: bundle growth (state the first-load JS budget: measure the current .next/static main chunks in a fresh build and set the ceiling), GPU class ladder (integrated → discrete), mobile thermal/DPR, SwiftShader limits, R3F/React 19, service-worker cache (build-stamped worker exists), fonts/MSDF generation, 4-core host build time.
- S-9 Write docs/architecture/CINEMATIC-VFX-v1-A.md (sections: 0 Owner's words & reading · 1 Removal register · 2 Direction · 3 Per-section spec · 4 Gate set CQ + rubric AQ · 5 TDD cases · 6 Slices · 7 Risk · 8 Prototype plan for t_w3_pa) and docs/architecture/CINEMATIC-VFX-TASKS-A.json (slices). Read-only for app code, tests and content. ≤ 30 min. Return {task_id:'t_w3_dA', direction:'A', doc, tasks_json, removal_rows:n, cq:[{id,threshold}], aq:[id…], tdd:n, slices:[{id,title,minutes,files}], first_ship_minutes, bundle_ceiling_kb, risks:[…], goal_complete:true}.

## QUALITY GATES
- Removal register covers every current visual element in all six sections + MiniVic chrome, each with an observation from the screenshots and a replacement
- Every immovable constraint addressed explicitly in the per-section spec (palette, copy, LCP/CLS, budgets, autoplay, reduced-motion, no-GL, a11y, G-MV1, ids, tests-replaced, O5)
- CQ gate set: ≥ 10 instrumented gates with methods and thresholds at two viewports; AQ rubric ≥ 8 criteria; carried instruments named
- TDD cases named before code with file/assertion/threshold
- Slices ≤ 30 min each, hero first, 10–16 rows, each visible, removal inside the replacing slice
- Doc + TASKS json written; no app code, test or content file edited

## VERIFICATION
```bash
test -f /root/forgotten-mistory/docs/architecture/CINEMATIC-VFX-v1-A.md && grep -c 'CQ-' /root/forgotten-mistory/docs/architecture/CINEMATIC-VFX-v1-A.md
node -e "const j=require('/root/forgotten-mistory/docs/architecture/CINEMATIC-VFX-TASKS-A.json');console.log(j.slices?j.slices.length:Object.keys(j).length)"
git status --porcelain -- app components lib tests | wc -l
```

## HIERARCHY
role_matrix: architecture / requirements_analysis → level 1 → effort **max** (effort_cascade.yaml; depth_cap 4). Model: claude-opus · Max OAuth. max_runtime_seconds 1800 (O1) · goal_max_turns 20.

## PROVIDER
Anthropic via OAuth (CLAUDE_CODE_OAUTH_TOKEN / claude-cli Max session). Never ANTHROPIC_API_KEY.

## STATUS (2026-09-06T05:43:10.400Z)
running — dispatched 05:43Z (fm-wave3-directions: two SAs opus/max in parallel, then serialized prototypes; fm-wave3-p0a: AP opus/xhigh)
