# t_28011b3d — Cycle 14a — restore /api/tts: ElevenLabs Secret Manager drift fixed, premade voice labelled synthetic (R3 voice, M5 prerequisite)

**Status:** todo · **Priority:** 94 · **Parents:** t_cba10f82 · **Created:** 2026-09-05T06:20:05.274Z

## YOUR ROLE
analyst-programmer — coding (docs/prompt.md §5). SPEC-v10.md §R3(a): live POST /api/tts → 502 {tts_upstream_failed, status 401} while ELEVENLABS_API_KEY in /root/.claude/.env.production authenticates (GET /v1/user/subscription → 200, tier payg, IVC/PVC false). Classified as Secret Manager drift, not a credit block. The cloned voice 0ZJ4kFDo6bZUNQsuULOW is refused on payg (ivc_not_permitted) — orchestrator decision (§0.1): the site speaks with an ElevenLabs premade (stock) voice and every place it is heard is labelled synthetic. Spend cap for this task: ONE verification call ≤ 200 characters (≈ $0.03 on payg); log the character count.

## PROJECT ROOT
/root/forgotten-mistory (VPS srv1356245). Evidence: docs/delivery/evidence/v10-20260905T0515Z/. Live: https://forgotten-mistory.web.app. Static servers already bound by other tenants: :5599 and :8080 — never reuse them; council batteries use :5601 / :5602.

## MANDATORY
Call kanban_complete() when ALL gates pass — i.e. return structured output {task_id, gates:{...}, files_changed:[...], evidence:[...], goal_complete:true}; the orchestrator writes it to the board. Never self-approve; never weaken a check to pass it; every claim cites a command or a path. No secrets in output — read keys by name from /root/.claude/.env.production with a `grep -E '^[A-Z][A-Z0-9_]*='` reader, never `source` it, never print values.

## EXECUTION ORDER
- S-1 TDD: write tests/e2e/avatar-voice.spec.ts — POST /api/tts {"text":"probe"} → 200, content-type audio/mpeg, body ≥ 8 kB; second test: the MiniVic/Listen UI shows a visible /synthetic/i label wherever the voice plays. Run against https://forgotten-mistory.web.app → red (502).
- S-2 Compare fingerprints only, never values: `firebase functions:secrets:access ELEVENLABS_API_KEY | tr -d "\n" | sha256sum` vs `grep -E "^ELEVENLABS_API_KEY=" /root/.claude/.env.production | cut -d= -f2- | tr -d "\n" | sha256sum`. If they differ: pipe the grep/cut output (never echoed) into `firebase functions:secrets:set ELEVENLABS_API_KEY --data-file=-`. Record both sha256 prefixes (8 chars) in docs/delivery/evidence/v10-20260905T0515Z/C14a-tts/07-decisions.md.
- S-3 Voice: with the key in a shell variable (grep reader, never printed) `curl -s -H "xi-api-key: $KEY" https://api.elevenlabs.io/v1/voices | jq ".voices[] | select(.category==\"premade\") | {voice_id,name,labels}"` → pick one adult male English premade voice; read functions/index.js to see how the voice id is resolved (secret/param/constant) and set it there; the function must never fall back to the cloned id.
- S-4 Redeploy only the TTS function (`firebase functions:list` for the name; `firebase deploy --only functions:<name>`); then ONE live probe: POST /api/tts {"text":"Hello — this is a synthetic voice reading for Vikram."} → 200 audio/mpeg; save status/headers/size (not the audio) to C14a-tts/04-probe.log.
- S-5 UI label: grep components/MiniVicBot.tsx and components/sections/Listen for the existing synthetic label; if the voice can be heard anywhere without it, add the label (tokens only). Battery (tsc/lint/audit/build) if any client file changed; commit `fix(tts): the voice speaks again — premade ElevenLabs voice, labelled synthetic`; push the branch.

## QUALITY GATES
- [ ] avatar-voice.spec.ts red before, green against the live site after
- [ ] secret fingerprints recorded (prefixes only); no secret value in any log, commit or output
- [ ] exactly one paid TTS call made; character count logged
- [ ] no cloned-voice usage remains (grep functions/ for 0ZJ4kFDo6bZUNQsuULOW → 0 hits)
- [ ] tsc/lint/audit/build green if client code changed

## VERIFICATION
```bash
curl -s -o /dev/null -w "%{http_code} %{content_type} %{size_download}\n" -X POST -H "content-type: application/json" -d '{"text":"probe"}' https://forgotten-mistory.web.app/api/tts
PLAYWRIGHT_BASE_URL=https://forgotten-mistory.web.app npx playwright test tests/e2e/avatar-voice.spec.ts
```

## HIERARCHY
role_matrix: coding → level 2 → effort **xhigh** (effort_cascade.yaml; depth_cap 4). Model: claude-opus · Max OAuth. max_runtime_seconds 1800 (O1) · goal_max_turns 20.

## PROVIDER
Anthropic via OAuth (CLAUDE_CODE_OAUTH_TOKEN / claude-cli Max session). Never ANTHROPIC_API_KEY.

## STATUS (2026-09-05T06:21:18.373Z)
running — Workflow lane C14a (analyst-programmer opus xhigh → reviewer opus max), worktree isolation

## DECISION (2026-09-05T06:21:18.460Z)
Spend decision (§0.1, C-4): exactly one ElevenLabs TTS verification call ≤ 200 characters (≈$0.03 on payg) is authorised for this task; recovery path for the production change: Secret Manager keeps the prior version (record its number) and the previous function revision can be redeployed from main.

## DECISION (2026-09-05T06:23:26.224Z)
06:2xZ: the first dispatch was denied by the claude-cli auto-mode classifier (the brief piped the ElevenLabs secret into Secret Manager). Not worked around. Re-scoped: diagnose from the function's own logs first (logger.error at functions/index.js:98 records the upstream detail); if the cause is the cloned-voice permission, switch VOICE_ID to a premade voice chosen via the ElevenLabs MCP (no key handling) and redeploy elevenLabsTts; if the cause is an invalid key, return blocked (capability) and the orchestrator handles the rotation as a named production action. No agent reads /root/.claude/.env.production in this task.

## COMMENT (2026-09-05T06:36:48.438Z)
06:38Z: the implementer's branch worktree-wf_55e925e9-074-1 was consolidated into main by deploy.yml (5a07ac0: 8c09833 fix(tts) premade voice labelled synthetic, 60a474f evidence, 3af523c refactor keeping the cloned id out of shipped code) before V-C14a ran — expected under the Owner's gate-free pipeline (O3); ledger rows appended after the fact; V-C14a verdict still gates the board close, and R-c13 probes /api/tts on the live build.
