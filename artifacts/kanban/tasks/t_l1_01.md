# t_l1_01 — Listen CTA — one door, and it books a 20-minute call (G-C1 / R4 P1, recruiter-visible in one window)

**Status:** todo · **Priority:** 95 · **Parents:** — · **Created:** 2026-09-05T15:19:23.671Z

## YOUR ROLE
analyst-programmer — coding (docs/prompt.md §5). [LISTEN-TASKS.json g-l1-01-listen-cta-agenda] ADV-1451Z: 'Dual mailto engage pills; no booking (P1)'. LISTEN-FLAGSHIP.md §3 settles it: there is NO booking tool on this account — `grep -E '^[A-Z][A-Z0-9_]*=' /root/.claude/.env.production | sed 's/=.*//' | sort -u` returns 45 key names and `| grep -ciE 'cal|calendar|book|schedul|meet'` returns 0. Do NOT invent a Cal.com/Calendly URL and do NOT print a response-time promise (app/data/siteContent.ts:512 forbids unverifiable promises; no sourced figure exists). Ship the honest achievable CTA instead: in app/data/portfolio/listen.ts change engage.label to 'Book a 20-minute call', engage.subject to a 20-minute-call subject, and add a prefilled body of exactly four agenda lines ('What you're building:', 'The decision you need made:', 'Two or three times that suit you (Melbourne time):', 'Anything I should read first:') encodeURIComponent'd into the mailto. Then demote the duplicate email pill in Listen.tsx/Listen.module.css from a filled plate to a plain channel so the section has exactly ONE filled action. DOM + data only — no GL, no new copy sentence.

## PROJECT ROOT
/root/forgotten-mistory (VPS srv1356245). Evidence: docs/delivery/evidence/v10-20260905T0515Z/. Live: https://forgotten-mistory.web.app. Static servers already bound by other tenants: :5599 and :8080 — never reuse them; council batteries use :5601 / :5602.

## MANDATORY
Call kanban_complete() when ALL gates pass — i.e. return structured output {task_id, gates:{...}, files_changed:[...], evidence:[...], goal_complete:true}; the orchestrator writes it to the board. Never self-approve; never weaken a check to pass it; every claim cites a command or a path. No secrets in output — read keys by name from /root/.claude/.env.production with a `grep -E '^[A-Z][A-Z0-9_]*='` reader, never `source` it, never print values.

## EXECUTION ORDER
- Read docs/architecture/LISTEN-FLAGSHIP.md §3 and app/data/portfolio/listen.ts
- Re-run the two env key-NAME greps yourself and paste both outputs into the evidence log; never source the file, never print a value
- Write tests/overhaul/listen-flagship.spec.ts with TC-LISTEN-CTA-01..04 first and capture them red (02-tests-failing.log)
- Edit listen.ts: engage.label/subject/body + href; keep href <= 900 chars encoded
- Edit Listen.tsx + Listen.module.css: the email channel drops styles.pill/pillRow and uses styles.channel like the other three
- Re-run tests/e2e/listen.spec.ts TC-LISTEN-02 (<=65 words) and TC-LISTEN-10 (routes span the column) and fix layout fallout, never the thresholds
- npm run build:static, serve out/, capture 04-tests-passing.log, push the branch

## QUALITY GATES
- No booking URL of any kind ships — grep -rniE 'cal\.com|calendly|savvycal' out/ returns 0
- No response-time promise — #listen text matches /within\s+\d+\s*(h|hours|business\s+days)/i zero times
- Exactly one [data-cta="engage"] filled plate in #listen; total anchors stay 5 so TC-SCENE-LISTEN-04/05 stay green
- mailto carries both subject= and body=; decoded body has >= 4 agenda lines; whole href <= 900 characters
- TC-LISTEN-02 word cap <= 65 still green; no threshold in any existing spec weakened
- No secret value from .env.production appears in any file, log or commit message

## VERIFICATION
```bash
npm run build:static
python3 -m http.server 5635 --directory out & sleep 2
PLAYWRIGHT_BASE_URL=http://127.0.0.1:5635 npx playwright test tests/overhaul/listen-flagship.spec.ts tests/e2e/listen.spec.ts
grep -rniE 'cal\.com|calendly|savvycal' out/ | wc -l   # expect 0
node scripts/validate/overhaul_static_audit.mjs
```

## HIERARCHY
role_matrix: coding → level 2 → effort **xhigh** (effort_cascade.yaml; depth_cap 4). Model: claude-opus · Max OAuth. max_runtime_seconds 1800 (O1) · goal_max_turns 20.

## PROVIDER
Anthropic via OAuth (CLAUDE_CODE_OAUTH_TOKEN / claude-cli Max session). Never ANTHROPIC_API_KEY.

## COMPLETE (2026-09-05T16:58:32.714Z)
SUPERSEDED by t_g2_c1b. 1556Z: 'Book a 20-minute call' mailto is the FAIL. Do not implement this task's original Book-label instructions.
