# t_x1_14 — Independent verification on the live URL — R2 / R5 / §0.3-1 scorecard

**Status:** todo · **Priority:** 88 · **Parents:** t_x1_13 · **Created:** 2026-09-05T12:24:54.144Z

## YOUR ROLE
reviewer — 3rd_party_independent_adversarial_review (docs/prompt.md §5). From docs/architecture/SIGNATURE-SCENES-TASKS.json (solutions-architect t_g_h2, commit 0b0bf02) — read docs/architecture/SIGNATURE-SCENES-v1.md first. After the chain deploys, verify against https://forgotten-mistory.web.app/ (not a local build): all 7 sceneIds present and passing the flagship bar, median rAF within budget at 1440 and 390, 4K backing store proven, HyperFrames present in package.json and absent from the critical path, R5 raster waiver list empty or honestly non-empty. Report FAIL for anything not proven by a command output you ran yourself.

## PROJECT ROOT
/root/forgotten-mistory (VPS srv1356245). Evidence: docs/delivery/evidence/v10-20260905T0515Z/. Live: https://forgotten-mistory.web.app. Static servers already bound by other tenants: :5599 and :8080 — never reuse them; council batteries use :5601 / :5602.

## MANDATORY
Call kanban_complete() when ALL gates pass — i.e. return structured output {task_id, gates:{...}, files_changed:[...], evidence:[...], goal_complete:true}; the orchestrator writes it to the board. Never self-approve; never weaken a check to pass it; every claim cites a command or a path. No secrets in output — read keys by name from /root/.claude/.env.production with a `grep -E '^[A-Z][A-Z0-9_]*='` reader, never `source` it, never print values.

## EXECUTION ORDER
- Fetch the live build-commit meta and confirm it matches the deployed HEAD
- Run the flagship, framerate and resolution suites against PLAYWRIGHT_BASE_URL=https://forgotten-mistory.web.app
- Read the R5 waiver list and report its contents verbatim
- Write the scorecard to docs/delivery/evidence/v10-20260905T0515Z/G-H2/09-verification.md, failures first

## QUALITY GATES
- Every verdict cites a command output captured in this run
- A software-rasteriser fps pass is reported as such, never as GPU proof
- No self-approval: the reviewer profile is not the implementer
- Any residual FAIL is stated plainly with its unblock — no narrowing of R2 or R5

## VERIFICATION
```bash
curl -s https://forgotten-mistory.web.app/ | grep -o 'build-commit[^>]*'
PLAYWRIGHT_BASE_URL=https://forgotten-mistory.web.app npx playwright test tests/overhaul/flagship-visibility.spec.ts tests/perf/scene-framerate.spec.ts tests/perf/resolution-independence.spec.ts
```

## HIERARCHY
role_matrix: 3rd_party_independent_adversarial_review → level 1 → effort **max** (effort_cascade.yaml; depth_cap 4). Model: claude-opus · Max OAuth. max_runtime_seconds 1800 (O1) · goal_max_turns 20.

## PROVIDER
Anthropic via OAuth (CLAUDE_CODE_OAUTH_TOKEN / claude-cli Max session). Never ANTHROPIC_API_KEY.
