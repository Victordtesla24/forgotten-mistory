# t_w3_rm1 — REMOVAL SLICE 0a (analyst-programmer, xhigh) — hero + About: unmount and delete the hero atmosphere scene (HeroAtmosphere.tsx, atmosphere.glsl.ts, the poster/blur/glow and the opaque name/copy plates) and the About compass/field (AboutField.tsx, Compass.tsx, field.glsl.ts) with their CSS; leave a disciplined black frame with the words, the photograph, the ledger and the ten dimensions fully readable; supersede the tests that asserted the removed elements with tests/overhaul/interim-frame.spec.ts; audit 10/10; push worktree-w3-rm1 within 30 min

**Status:** ready · **Priority:** 100 · **Parents:** t_w3_own · **Created:** 2026-09-06T05:55:20.949Z

## YOUR ROLE
analyst-programmer — coding (docs/prompt.md §5). Owner 05:51Z (verbatim in artifacts/kanban/INBOX/OWNER-DIRECTIVE-20260906T0529Z.md §Escalation): 'remove all your shabby work immediately and replace it with credible work'. This slice is the REMOVAL: every current effect and decoration goes now; nothing new is invented here (the credible replacement is specified under CINEMATIC-VFX-v1 and lands in later slices). The interim frame is disciplined: pure black ground (the existing near-black tokens; if the darkest token is not ≤ 0.03 luma add one token in app/globals.css :root, never raw hex elsewhere), white/grey type from the existing tokens, the monochrome photograph as a plain image with no blur/glow/plate, every word of app/data/portfolio/*.ts unchanged, section ids and order unchanged, every caliper mark unchanged, MiniVic unchanged, nav unchanged. Zero functional regression: tests that asserted a removed element are SUPERSEDED — each is listed in docs/architecture/INTERIM-FRAME.md with its replacement in tests/overhaul/interim-frame.spec.ts and removed from the suite in the same commit (git history keeps it); the static audit stays 10/10 — if a gate counts scene mounts or canvases, re-point that gate to the interim contract with the reason written in INTERIM-FRAME.md (never a silent weakening). Work in a fresh worktree from origin/main (git -C /root/forgotten-mistory worktree add .claude/worktrees/<slug> -b worktree-<slug> origin/main; npm ci if node_modules is missing); one heavy job at a time; never run git checkout/reset/stash in /root/forgotten-mistory; wait on PIDs, never on a process-name search containing your own command text; stop your static server with kill <PID>. Slug: w3-rm1. Port 5606.

## PROJECT ROOT
/root/forgotten-mistory (VPS srv1356245). Evidence: docs/delivery/evidence/v10-20260905T0515Z/. Live: https://forgotten-mistory.web.app. Static servers already bound by other tenants: :5599 and :8080 — never reuse them; council batteries use :5601 / :5602.

## MANDATORY
Call kanban_complete() when ALL gates pass — i.e. return structured output {task_id, gates:{...}, files_changed:[...], evidence:[...], goal_complete:true}; the orchestrator writes it to the board. Never self-approve; never weaken a check to pass it; every claim cites a command or a path. No secrets in output — read keys by name from /root/.claude/.env.production with a `grep -E '^[A-Z][A-Z0-9_]*='` reader, never `source` it, never print values.

## EXECUTION ORDER
- R-1 Read: the directive note (both entries) and the Owner's hero/About screenshots; components/sections/Hero/{Hero.tsx,Hero.module.css,HeroPortrait.tsx,HeroAtmosphere.tsx,atmosphere.glsl.ts}, components/sections/About/{About.tsx,AboutField.tsx,Compass.tsx,field.glsl.ts,*.module.css}, components/gl/Scene.tsx, app/globals.css (hero/about rules and tokens), app/page.tsx; then grep -rln 'HeroAtmosphere|atmosphere|data-plane|hero-atmosphere-poster|AboutField|Compass|data-scene="about|instrumentStage|story' tests scripts app components lib — every consumer and every contract.
- R-2 TDD first — tests/overhaul/interim-frame.spec.ts (Playwright, tags TC-IF-01…): (01) #hero and #about contain no canvas and no [data-scene]/[data-plane] element; (02) at 1440x900, 1280x800, 834x1194, 390x844 the first fold shows the name, the role line, the sentence and both CTAs, and the photograph's img/video element is visible with no CSS filter/backdrop-filter and no plate behind the text (no ancestor of the H1 with an opaque background other than the page ground); (03) hero ground luma ≤ 0.03 at nine sample points away from text and photo (screenshot + sharp); (04) every text node in #hero/#about ≥ 4.5:1 contrast (reuse tests/a11y/text-contrast helpers); (05) #about shows the heading, the two intro paragraphs, the dimensions source line and ten dimension rows (7 answered with evidence lines, 3 open with their reason) — all readable, no canvas; (06) ?gl=force → 0 pageerrors and 0 canvases in #hero/#about; (07) prefers-reduced-motion renders the same frame (pixel diff ≤ 0.5 %); (08) chroma 0 everywhere in #hero/#about except elements carrying the gold claim class; (09) the ledger figures render below the fold with their caliper marks unchanged; (10) TC-MV-CLICK-01 first-fold click still passes at 390. Run it red before removing anything.
- R-3 Remove, in one commit with R-2's replacements: delete components/sections/Hero/HeroAtmosphere.tsx and atmosphere.glsl.ts, the poster/blur/glow/plate rules in Hero.module.css and app/globals.css, the plate wrappers in Hero.tsx, the poster asset public/assets/hero-atmosphere-poster.avif if nothing references it afterwards; delete components/sections/About/AboutField.tsx, Compass.tsx, field.glsl.ts and their CSS (instrumentStage, fieldViewport, keyRow…), unmount from About.tsx, keep the ten rows and the dimensions caption. Keep HeroPortrait as a plain monochrome still (poster/still from app/data/portfolio/avatar.ts; no autoplay; no filter). Dead CSS must go (TC-NFR-DEADCSS). Remove the superseded specs named in R-1 (hero-setpiece, hero-plane-dominance, hero atmosphere/poster specs, scene-about, About story-contract rows, flagship-visibility HERO/ABOUT rows, monochrome hero-palette if it targets the plane) and write docs/architecture/INTERIM-FRAME.md: removed elements table, superseded test → replacement table, audit gate changes with reasons, what the wave-3 slices reinstate (SPD instrument, story metric) — the instrument scripts under scripts/validate stay in place for the new hero.
- R-4 Verify: npx tsc --noEmit · npm run lint · npm run build:static · node scripts/validate/overhaul_static_audit.mjs (10/10 — re-point a scene-count gate to the interim contract only with the reason in INTERIM-FRAME.md) · serve out/ on :5606 · PLAYWRIGHT_BASE_URL=http://127.0.0.1:5606 npx playwright test tests/overhaul/interim-frame.spec.ts tests/e2e tests/a11y tests/content tests/perf tests/monochrome tests/overhaul — fix or supersede-with-reason every red (never skip); node --test tests/*.test.mjs. Full logs (never truncated) + screenshots of #hero and #about at the four viewports → docs/delivery/evidence/v10-20260905T0515Z/W3-RM1/.
- R-5 Ledger row (node scripts/pm/ledger_append.mjs --task t_w3_rm1 --role analyst-programmer --model claude-opus --prompt artifacts/kanban/tasks/t_w3_rm1.md --range <sha> --cached false --cwd <worktree> -- <files>) before the commit; conventional commit whose body lists every deleted file and every superseded spec with its replacement; push origin HEAD:refs/heads/worktree-w3-rm1 (never main). ≤ 30 min: if you would overrun, ship the hero half green and pushed and report the About half as remaining. Return {task_id:'t_w3_rm1', branch, sha, pushed, files_deleted:[…], files_changed:[…], specs_superseded:[{old,replacement}], audit_gate_changes:[…], gates:{tsc,lint,build,audit,playwright,node_test}, evidence:[…], remaining:[…], goal_complete:true}.

## QUALITY GATES
- interim-frame.spec.ts TC-IF-01…10 green on the export; every other suite green or superseded with a written reason
- No canvas, no plate, no blur in #hero/#about; words, photograph, ledger, ten rows unchanged and readable at four viewports
- tsc/lint/build clean; audit 10/10 with any gate change documented in INTERIM-FRAME.md
- Dead CSS removed; deleted files listed in the commit body; ledger row present; branch pushed

## VERIFICATION
```bash
git -C /root/forgotten-mistory/.claude/worktrees/w3-rm1 diff --stat origin/main..HEAD | tail -1
test -f /root/forgotten-mistory/.claude/worktrees/w3-rm1/docs/architecture/INTERIM-FRAME.md
ls /root/forgotten-mistory/docs/delivery/evidence/v10-20260905T0515Z/W3-RM1/ | wc -l
```

## HIERARCHY
role_matrix: coding → level 2 → effort **xhigh** (effort_cascade.yaml; depth_cap 4). Model: claude-opus · Max OAuth. max_runtime_seconds 1800 (O1) · goal_max_turns 20.

## PROVIDER
Anthropic via OAuth (CLAUDE_CODE_OAUTH_TOKEN / claude-cli Max session). Never ANTHROPIC_API_KEY.

## STATUS (2026-09-06T05:55:54.618Z)
running — dispatched 05:56Z fm-wave3-removal (AP opus/xhigh, port 5606)

## COMMENT (2026-09-06T06:11:22.548Z)
pushed a92f85b (worktree-w3-rm1) 06:10Z, 14 min after dispatch: 39 files, 7683 deletions (HeroAtmosphere, atmosphere.glsl, AboutField, Compass, field.glsl, the poster, the plates); interim-frame.spec 31/31 on the export per the commit body; 9 specs/rows superseded with replacements listed in INTERIM-FRAME.md; audit 10/10 with no gate re-pointed. Consolidation pending; the reviewer grades it on live (rev8 if it catches the deploy, else rev9). Awaiting the agent's structured return for the full-suite log status.

## COMMENT (2026-09-06T06:32:34.633Z)
06:31Z second push d2cf5ba: remaining red rows superseded with reasons (hero-palette/hero-contrast gl waits, about compass rows, hero-photo plane/bloom rows, TC-HERO-21); six section suites 65/66 on the export; the one red is TC-HERO-20 LCP 2996 ms at 1280x720 on a host at load 21 — pre-existing on the pre-change export too; recorded, not claimed. rev9 measures LCP on live.
