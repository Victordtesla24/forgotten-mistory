# t_x1_03b — P1 — the 390 reduced-motion hero still clears TC-HERO-SCRIM-01 by 0.004 (Δ 0.064 vs floor 0.06 on live): give it ≥ 0.03 margin without touching AA

**Status:** todo · **Priority:** 75 · **Parents:** — · **Created:** 2026-09-05T14:36:53.204Z

## YOUR ROLE
analyst-programmer — coding (docs/prompt.md §5). Reviewer G-REV/66199cba: live Δ on the 390 reduced-motion still = 0.064 (lane claimed 0.0776) — one shader/AVIF phase away from red. Raise the right-edge pool of the 390 still (Hero.module.css ≤700px .stage layers, now stacked over the 4K poster) so Δ ≥ 0.09 at 390 still, keeping the phone plates' AA (text-contrast both paths) and the still floors; also reconcile SIGNATURE-SCENES-v1.md §4.1(b) (outer-thirds geometry + 0.72 alpha) with the shipped DOM-measured gate and the WCAG reasoning in tests/overhaul/hero-first-paint.spec.ts.

## PROJECT ROOT
/root/forgotten-mistory (VPS srv1356245). Evidence: docs/delivery/evidence/v10-20260905T0515Z/. Live: https://forgotten-mistory.web.app. Static servers already bound by other tenants: :5599 and :8080 — never reuse them; council batteries use :5601 / :5602.

## MANDATORY
Call kanban_complete() when ALL gates pass — i.e. return structured output {task_id, gates:{...}, files_changed:[...], evidence:[...], goal_complete:true}; the orchestrator writes it to the board. Never self-approve; never weaken a check to pass it; every claim cites a command or a path. No secrets in output — read keys by name from /root/.claude/.env.production with a `grep -E '^[A-Z][A-Z0-9_]*='` reader, never `source` it, never print values.

## EXECUTION ORDER
- S-1 measure on a fresh build (TC-HERO-SCRIM-01 [390, still]) 5×; S-2 reshape/raise the still's pool; S-3 push on tsc/lint/build/audit green; battery + screenshots follow; S-4 docs reconciliation commit (architecture note).

## QUALITY GATES
- Δ ≥ 0.09 at 390 still over 5 runs; other three surfaces unchanged ± 0.02
- text-contrast both paths green; still floors hold
- ledger; pushed

## VERIFICATION
```bash
PLAYWRIGHT_BASE_URL=http://127.0.0.1:5626 npx playwright test tests/overhaul/hero-first-paint.spec.ts tests/a11y/text-contrast.spec.ts --workers=1 --repeat-each=3
```

## HIERARCHY
role_matrix: coding → level 2 → effort **xhigh** (effort_cascade.yaml; depth_cap 4). Model: claude-opus · Max OAuth. max_runtime_seconds 1800 (O1) · goal_max_turns 20.

## PROVIDER
Anthropic via OAuth (CLAUDE_CODE_OAUTH_TOKEN / claude-cli Max session). Never ANTHROPIC_API_KEY.
