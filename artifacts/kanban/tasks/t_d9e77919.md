# t_d9e77919 — Cycle 15b — #experience polish: stagger to the R-c13 spec (60 ms + 70 ms roll), .trackYears opacity roll-in as each bar stops, fix the 2025/now axis-label overlap at 390

**Status:** todo · **Priority:** 72 · **Parents:** t_86cdd156 · **Created:** 2026-09-05T08:25:47.491Z

## YOUR ROLE
coder — coding / documentation (docs/prompt.md §5). V-c15 findings F-1/F-2/F-3 (906ccd5 09-verification.md). Files: components/sections/Experience/Experience.module.css (.trackBar delay calc, .trackYears opacity transition calc(var(--i) * 70ms + 620ms) 320 ms, .axis/.axisTick spacing at ≤30rem), Experience.tsx if --i is missing; tests/overhaul/scene-experience.spec.ts gains the two assertions + a 390 axis-label non-overlap assertion (getBoundingClientRect of the last two axis labels).

## PROJECT ROOT
/root/forgotten-mistory (VPS srv1356245). Evidence: docs/delivery/evidence/v10-20260905T0515Z/. Live: https://forgotten-mistory.web.app. Static servers already bound by other tenants: :5599 and :8080 — never reuse them; council batteries use :5601 / :5602.

## MANDATORY
Call kanban_complete() when ALL gates pass — i.e. return structured output {task_id, gates:{...}, files_changed:[...], evidence:[...], goal_complete:true}; the orchestrator writes it to the board. Never self-approve; never weaken a check to pass it; every claim cites a command or a path. No secrets in output — read keys by name from /root/.claude/.env.production with a `grep -E '^[A-Z][A-Z0-9_]*='` reader, never `source` it, never print values.

## EXECUTION ORDER
- S-1 TDD: extend scene-experience.spec.ts (stagger delays per R-c13 MOT-C13-02; .trackYears opacity transition present and lands after its bar; at 390 the 2025 and now labels do not intersect) → red
- S-2 CSS-only implementation; reduced motion unchanged
- S-3 battery; commit `feat(experience): numbers land as their bars stop`; push

## QUALITY GATES
- [ ] specs red → green
- [ ] battery green
- [ ] no gold

## VERIFICATION
```bash
PLAYWRIGHT_BASE_URL=http://127.0.0.1:5601 npx playwright test tests/overhaul/scene-experience.spec.ts tests/e2e/experience.spec.ts
```

## HIERARCHY
role_matrix: coding / documentation → level 4 → effort **medium (xhigh when coding)** (effort_cascade.yaml; depth_cap 4). Model: deepseek · OpenRouter → Anthropic OAuth (§0.4 failover). max_runtime_seconds 1800 (O1) · goal_max_turns 20.

## PROVIDER
OpenRouter (OPENROUTER_API_KEY) — balance negative (402), so this task runs on Anthropic via OAuth (CLAUDE_CODE_OAUTH_TOKEN / claude-cli session). Never ANTHROPIC_API_KEY.
