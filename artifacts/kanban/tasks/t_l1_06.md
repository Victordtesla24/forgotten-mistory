# t_l1_06 — Independent verification on live — #listen flagship + CTA, against ADV-1451Z (O2/O6)

**Status:** todo · **Priority:** 89 · **Parents:** t_l1_05 · **Created:** 2026-09-05T15:19:23.970Z

## YOUR ROLE
reviewer — 3rd_party_independent_adversarial_review (docs/prompt.md §5). [LISTEN-TASKS.json g-l1-06-independent-live-verify] 3rd-party adversarial pass on https://forgotten-mistory.web.app AFTER the deploy that carries slices 01-05, not on a local build. Confirm or refute, each with a command or a screenshot: (a) the ADV-1451Z P0 'deliberately sparse caliper field' — does #listen now show a subject a viewer can name in one sentence? (b) the P1 'dual mailto engage pills' — is there exactly one filled action, and does it prefill an agenda? (c) no invented booking link anywhere; (d) no unsourced response-time promise; (e) gold appears on exactly the two record marks. Report FAIL loudly if any clause is cosmetic-only. Do NOT mark G-L1 closed without a live build-commit meta matching the shipped commit.

## PROJECT ROOT
/root/forgotten-mistory (VPS srv1356245). Evidence: docs/delivery/evidence/v10-20260905T0515Z/. Live: https://forgotten-mistory.web.app. Static servers already bound by other tenants: :5599 and :8080 — never reuse them; council batteries use :5601 / :5602.

## MANDATORY
Call kanban_complete() when ALL gates pass — i.e. return structured output {task_id, gates:{...}, files_changed:[...], evidence:[...], goal_complete:true}; the orchestrator writes it to the board. Never self-approve; never weaken a check to pass it; every claim cites a command or a path. No secrets in output — read keys by name from /root/.claude/.env.production with a `grep -E '^[A-Z][A-Z0-9_]*='` reader, never `source` it, never print values.

## EXECUTION ORDER
- Probe the live build-commit meta and record it with a timestamp
- Navigate live at 1440 and 390, screenshot #listen in normal and ?gl=force contexts
- Read the section aloud in one sentence: if the field's subject cannot be named, that is a FAIL, not a nit
- Check the mailto by copying the href out of the live DOM and decoding subject+body
- grep the served bundle for cal.com|calendly|savvycal and for a response-time promise
- Write docs/delivery/evidence/<cycle>/G-L1/08-adversarial-review.md with a false-positive register

## QUALITY GATES
- Verdict cites the live build-commit and the probe timestamp — never a local build
- Every claim carries the command or screenshot path that produced it
- Anything not reproducible is recorded in a false-positive register, not asserted either way
- Reviewer does not edit production code

## VERIFICATION
```bash
curl -s https://forgotten-mistory.web.app | grep -o 'build-commit[^>]*' | head -1
python3 -m http.server 5640 --directory out & sleep 2   # only if a local A/B is needed
PLAYWRIGHT_BASE_URL=https://forgotten-mistory.web.app npx playwright test tests/overhaul/listen-flagship.spec.ts
```

## HIERARCHY
role_matrix: 3rd_party_independent_adversarial_review → level 1 → effort **max** (effort_cascade.yaml; depth_cap 4). Model: claude-opus · Max OAuth. max_runtime_seconds 1800 (O1) · goal_max_turns 20.

## PROVIDER
Anthropic via OAuth (CLAUDE_CODE_OAUTH_TOKEN / claude-cli Max session). Never ANTHROPIC_API_KEY.
