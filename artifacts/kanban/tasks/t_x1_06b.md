# t_x1_06b — P1 — listen-field floors have thin margins (motion 1.07× at 1440 on live; both widths red in a loaded worktree battery): give the scene ≥ 1.3× margin on every floor without breaking AA under the Listen copy

**Status:** todo · **Priority:** 72 · **Parents:** — · **Created:** 2026-09-05T15:10:23.608Z

## YOUR ROLE
analyst-programmer — coding (docs/prompt.md §5). Reviewer t_rev_s56 (live ff67273b): listen-field 1440 motion 0.00428 vs floor 0.004; the shader-cost lane's worktree battery under load 11 showed listen-field failing at both widths. A scene that passes only on a quiet host is not proven. Raise the field's motion/peak so each floor is cleared by ≥ 1.3× at 1440 and 390 (measure 5× on a loaded host, record load), keep the still ≥ 8%, AA of #listen text on both paths, zero gold. ≤ 30 min.

## PROJECT ROOT
/root/forgotten-mistory (VPS srv1356245). Evidence: docs/delivery/evidence/v10-20260905T0515Z/. Live: https://forgotten-mistory.web.app. Static servers already bound by other tenants: :5599 and :8080 — never reuse them; council batteries use :5601 / :5602.

## MANDATORY
Call kanban_complete() when ALL gates pass — i.e. return structured output {task_id, gates:{...}, files_changed:[...], evidence:[...], goal_complete:true}; the orchestrator writes it to the board. Never self-approve; never weaken a check to pass it; every claim cites a command or a path. No secrets in output — read keys by name from /root/.claude/.env.production with a `grep -E '^[A-Z][A-Z0-9_]*='` reader, never `source` it, never print values.

## EXECUTION ORDER
- S-1 measure 5× with load recorded; S-2 TDD margin assertion in flagship-visibility (≥1.3× for listen only if the spec supports per-scene margins — otherwise assert in scene-listen.spec); S-3 raise; S-4 PUSH RULE; evidence.

## QUALITY GATES
- ≥1.3× every floor at 1440/390 over 5 runs; still ≥ 8%; AA green; 0 gold
- ledger; pushed

## VERIFICATION
```bash
PLAYWRIGHT_BASE_URL=http://127.0.0.1:5636 npx playwright test tests/overhaul/flagship-visibility.spec.ts -g LISTEN --repeat-each=3 --workers=1
```

## HIERARCHY
role_matrix: coding → level 2 → effort **xhigh** (effort_cascade.yaml; depth_cap 4). Model: claude-opus · Max OAuth. max_runtime_seconds 1800 (O1) · goal_max_turns 20.

## PROVIDER
Anthropic via OAuth (CLAUDE_CODE_OAUTH_TOKEN / claude-cli Max session). Never ANTHROPIC_API_KEY.
