# t_c16b0001 — Cycle 16b — MiniVic correction: open panel never covers the H1 at 1280×800/1366×768, accessible name contains the visible label, a permanent white mark under the portrait at ≤834, no source-less <video> (V-c16 F-V16-1/2 + R-c13 CC-03a)

**Status:** running · **Priority:** 92 · **Parents:** t_cc03ed93 · **Created:** 2026-09-05T07:31:25.195Z

## YOUR ROLE
analyst-programmer — coding (docs/prompt.md §5). V-c16 (b712c3d) FAIL on 2 of 13 lines: (F-V16-2) at 1280×800 and 1366×768 the open panel {l:824,t:232,r:1256,b:712} intersects #hero h1 glyph rects (~135×69 px of the name covered; TC-BOT-12 only checks the block box); (F-V16-1) the launcher box still overlaps <p>/<li> boxes in #about at 390 (luminance is now safe — dark plate — so the remaining question is legibility of the covered words, not contrast). Non-blocking: WCAG 2.5.3 Label in Name — visible pill says Ask Mini Vic while the accessible name is Open Mini Vic assistant; at 390 the affordance is a near-black disc with a faint face and no word. R-c13 CC-03a: never ship a source-less <video> (render it only with a resolved source, poster first) and give the button a permanent 24×24 white mark (speech-mark svg or VIC monogram) rendered UNDER the video as the resting state.

## PROJECT ROOT
/root/forgotten-mistory (VPS srv1356245). Evidence: docs/delivery/evidence/v10-20260905T0515Z/. Live: https://forgotten-mistory.web.app. Static servers already bound by other tenants: :5599 and :8080 — never reuse them; council batteries use :5601 / :5602.

## MANDATORY
Call kanban_complete() when ALL gates pass — i.e. return structured output {task_id, gates:{...}, files_changed:[...], evidence:[...], goal_complete:true}; the orchestrator writes it to the board. Never self-approve; never weaken a check to pass it; every claim cites a command or a path. No secrets in output — read keys by name from /root/.claude/.env.production with a `grep -E '^[A-Z][A-Z0-9_]*='` reader, never `source` it, never print values.

## EXECUTION ORDER
- S-1 TDD: extend tests/e2e/chatbot.spec.ts TC-BOT-12 (or add TC-BOT-14) to assert with Range.getClientRects() that no glyph rect of #hero h1 intersects the open panel at 1440×900, 1366×768, 1280×800; a11y spec: the toggle accessible name matches /ask mini vic/i (label-in-name) and the visible pill text is a prefix of it; affordance spec: [data-testid=minivic-toggle] contains an svg or non-empty innerText at 390 and 1440; every <video> inside it has non-empty currentSrc OR a poster attribute. Run → red.
- S-2 Panel: max-height min(34rem, 100vh − 12rem) and bottom offset so the top edge clears the h1 bottom (284 px at 1280) with ≥ 16 px; or shift the panel left edge past the h1 right edge — measure, do not guess. Launcher: aria-label Ask Mini Vic — Vikram's AI clone (visible label first); a 24×24 var(--white) inline svg speech-mark under the portrait video, always present; <video> rendered only when avatar source resolves, with poster; at ≤834 keep the dark plate and show the mark.
- S-3 Battery; tests/e2e/chatbot.spec.ts, tests/a11y, tests/monochrome, tests/a11y/text-contrast.spec.ts (must stay 2 passed); screenshots closed 1440/390 + open 1280×800/1366×768; commit; push.

## QUALITY GATES
- [ ] new specs red → green
- [ ] TC-CONTRAST-01 still 2 passed
- [ ] monochrome + a11y green
- [ ] battery green

## VERIFICATION
```bash
PLAYWRIGHT_BASE_URL=http://127.0.0.1:5602 npx playwright test tests/e2e/chatbot.spec.ts tests/a11y tests/monochrome
```

## HIERARCHY
role_matrix: coding → level 2 → effort **xhigh** (effort_cascade.yaml; depth_cap 4). Model: claude-opus · Max OAuth. max_runtime_seconds 1800 (O1) · goal_max_turns 20.

## PROVIDER
Anthropic via OAuth (CLAUDE_CODE_OAUTH_TOKEN / claude-cli Max session). Never ANTHROPIC_API_KEY.
