# t_x1_01d — R2 — the About section's per-frame cost is mostly the PAGE composite at #about (~60%), not the field shader: find and remove the compositing tax (large blurred/filtered layers, backdrop-filter, animated box-shadows, will-change abuse, oversized SVG compass repaints) so about-field's harness median improves ≥ 3× from the 333 ms baseline

**Status:** todo · **Priority:** 85 · **Parents:** t_x1_01b · **Created:** 2026-09-05T14:54:34.375Z

## YOUR ROLE
analyst-programmer — coding (docs/prompt.md §5). From t_x1_01b's attribution probe (docs/delivery/evidence/v10-20260905T0515Z/G-X1-01b/03b-about-attribution.md): the about-field canvas is 384×384 yet costs 1866 ns/px — ~60% of the frame is the browser compositing/painting #about itself (Compass SVG with stroke-under-fill paint-order + filters? backdrop-filter plates? box-shadow rims per frame?) and ~40% compositing the canvas. Profile with Chrome tracing (Playwright CDP Tracing.start, categories disabled-by-default-devtools.timeline + blink) at /?gl=force with #about centred: name the top paint/composite consumers by DOM node; fix the layout/paint causes (promote or demote layers deliberately, cache static SVG as an image where legal, remove per-frame filter work, contain: paint on the card) without changing the picture (before/after screenshots inspected) or AA (text-contrast both paths). Prove ≥ 3× on the harness for about-field with load recorded. No 60 fps claim.

## PROJECT ROOT
/root/forgotten-mistory (VPS srv1356245). Evidence: docs/delivery/evidence/v10-20260905T0515Z/. Live: https://forgotten-mistory.web.app. Static servers already bound by other tenants: :5599 and :8080 — never reuse them; council batteries use :5601 / :5602.

## MANDATORY
Call kanban_complete() when ALL gates pass — i.e. return structured output {task_id, gates:{...}, files_changed:[...], evidence:[...], goal_complete:true}; the orchestrator writes it to the board. Never self-approve; never weaken a check to pass it; every claim cites a command or a path. No secrets in output — read keys by name from /root/.claude/.env.production with a `grep -E '^[A-Z][A-Z0-9_]*='` reader, never `source` it, never print values.

## EXECUTION ORDER
- S-1 trace + attribute (CDP tracing) → 01-attribution.json/md
- S-2 TDD: harness baseline for about-field (same host, load) → 02
- S-3 fix the named consumers; re-measure; screenshots
- S-4 PUSH RULE: tsc/lint/build/audit green → ledger → commit → push (merge origin/main first); battery follow-up

## QUALITY GATES
- about-field harness median ≥ 3× better (load recorded); other scenes not worse
- flagship floors + scene-about + text-contrast both paths green; picture unchanged (screenshots)
- ledger; pushed

## VERIFICATION
```bash
SCENE_FPS_ARTEFACT_DIR=docs/delivery/evidence/v10-20260905T0515Z/G-X1-01d PLAYWRIGHT_BASE_URL=http://127.0.0.1:5628 npx playwright test tests/perf/scene-framerate.spec.ts tests/overhaul/scene-about.spec.ts --workers=1
```

## HIERARCHY
role_matrix: coding → level 2 → effort **xhigh** (effort_cascade.yaml; depth_cap 4). Model: claude-opus · Max OAuth. max_runtime_seconds 1800 (O1) · goal_max_turns 20.

## PROVIDER
Anthropic via OAuth (CLAUDE_CODE_OAUTH_TOKEN / claude-cli Max session). Never ANTHROPIC_API_KEY.
