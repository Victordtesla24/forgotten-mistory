# t_x1_02c — CORRECTION — G-H2a: the first paint carries a REAL rendered still of the hero atmosphere (url() poster ≥ 0.10 mean luminance, ≤ 500 kB, 3840×2160 source) — not gradient layers; acceptance restored to the task's ≥ 0.10

**Status:** todo · **Priority:** 96 · **Parents:** t_x1_03, t_nojs01 · **Created:** 2026-09-05T14:02:43.429Z

## YOUR ROLE
analyst-programmer — coding (docs/prompt.md §5). ## CORRECTION: reviewer FAIL on G-H2a (docs/delivery/evidence/v10-20260905T0515Z/G-REV/e3f0206c/08-adversarial-review.md). Original output: 9dfaacd claimed 'priority scene over a lit poster'; live .stage background-image has NO url() layer (five gradients), luminance with the GL chunk blocked 0.053 @1440 / 0.081 @390; the lane lowered the floor to 0.04 where t_x1_02's acceptance says ≥ 0.10 — renegotiating the bar is the root cause #3 of the original FAIL review and is not accepted. Required fix: render a real frame of the hero atmosphere as the poster — capture the shader at frame 0 (or its brightest resting phase) at 3840×2160 with headless Chrome (SwiftShader GL_ARGS from tests/overhaul/flagship-visibility.spec.ts; or the HyperFrames engine path from docs/architecture/SIGNATURE-SCENES-v1.md t_x1_10 if it is faster to stand up — same result, a deterministic still of the same picture), encode AVIF (fallback WebP) ≤ 500 kB (asset budget), commit under public/assets/ with a reproducible script under scripts/assets/ (poster regenerates from the shader, never hand-painted), set it as the first background layer of .stage (url() then the existing gradients), keep the crossfade; the reduced-motion still and the no-GL path show the same picture (the photograph stays in colour; text plates keep AA — re-run text-contrast both paths). Mean luminance of the stage box with the GL chunk blocked ≥ 0.10 at 1440 and 390 (TC-HERO-FIRSTPAINT-01 floor raised to 0.10, no lower); with JS disabled (after t_nojs01) the poster paints. LCP < 2.5 s, CLS < 0.05 unchanged.

## PROJECT ROOT
/root/forgotten-mistory (VPS srv1356245). Evidence: docs/delivery/evidence/v10-20260905T0515Z/. Live: https://forgotten-mistory.web.app. Static servers already bound by other tenants: :5599 and :8080 — never reuse them; council batteries use :5601 / :5602.

## MANDATORY
Call kanban_complete() when ALL gates pass — i.e. return structured output {task_id, gates:{...}, files_changed:[...], evidence:[...], goal_complete:true}; the orchestrator writes it to the board. Never self-approve; never weaken a check to pass it; every claim cites a command or a path. No secrets in output — read keys by name from /root/.claude/.env.production with a `grep -E '^[A-Z][A-Z0-9_]*='` reader, never `source` it, never print values.

## EXECUTION ORDER
- S-1 Read the reviewer report, t_x1_02.md, SIGNATURE-SCENES-v1.md S1 + R5 plan, Hero.module.css .stage family (after t_x1_03), HeroAtmosphere.tsx + atmosphere.glsl.ts, scripts/build/*, tests/overhaul/hero-first-paint.spec.ts.
- S-2 TDD: raise TC-HERO-FIRSTPAINT-01's floor to 0.10 and add an assertion that .stage's computed background-image contains a url(/assets/…) layer → RED.
- S-3 Build the capture script (deterministic seed/time uniform), render 3840×2160, encode, budget check (< 500 kB; static audit asset gate), wire the CSS.
- S-4 PUSH RULE (RECTIFY): tsc + lint + build:static + audit 10/10 → ledger → commit → push (merge origin/main first). Then hero-first-paint + flagship-visibility (hero) + text-contrast (both paths) + performance + render → follow-up evidence commit with the poster shown at 1440/390 (JS on, GL blocked, JS off).

## QUALITY GATES
- url() poster layer present; stage luminance ≥ 0.10 at 1440 and 390 with the GL chunk blocked; poster ≤ 500 kB, ≥ 3840×2160 source, regenerable by script
- text-contrast both paths green; flagship floors hold; LCP < 2.5 s; CLS < 0.05; reduced-motion shows the still
- tsc, lint, audit 10/10; ledger; pushed

## VERIFICATION
```bash
PLAYWRIGHT_BASE_URL=http://127.0.0.1:5622 npx playwright test tests/overhaul/hero-first-paint.spec.ts tests/a11y/text-contrast.spec.ts tests/perf/performance.spec.ts --workers=1
ls -la public/assets/ | grep -i poster
```

## HIERARCHY
role_matrix: coding → level 2 → effort **xhigh** (effort_cascade.yaml; depth_cap 4). Model: claude-opus · Max OAuth. max_runtime_seconds 1800 (O1) · goal_max_turns 20.

## PROVIDER
Anthropic via OAuth (CLAUDE_CODE_OAUTH_TOKEN / claude-cli Max session). Never ANTHROPIC_API_KEY.

## STATUS (2026-09-05T14:10:29.400Z)
running — dispatched 14:11Z — fresh analyst-programmer, isolated worktree, port 5622; parents t_x1_03 (9e41474) and t_nojs01 (6f4ba6c) are on main

## COMPLETE (2026-09-05T14:36:52.956Z)
DONE — reviewer PASS on live (t_rev_h2_next): rendered 4K shader frame as the first .stage layer, luminance ≥ 0.10 on every path, 12.9 kB.
