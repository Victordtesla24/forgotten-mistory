# t_h2_04 — M3 — the photograph loses its card: drafting frame out, edges masked into the light

**Status:** todo · **Priority:** 95 · **Parents:** t_h2_03 · **Created:** 2026-09-05T15:19:23.453Z

## YOUR ROLE
analyst-programmer — coding (docs/prompt.md §5). [HERO-TASKS.json g2h1-04-photo-into-the-plane] HERO-FOLD-v2 §5.1 / decision D-3. HeroPortrait.tsx draws .portraitFrame (a hairline inset 12px), four .portraitTick corners and .portraitCross — a closed rectangle with registration marks parked in a second column, which is exactly what the 1451Z reviewer called 'a framed card beside a column'. Remove those three from the FOLD only (the drafting language stays on the caliper mark and in the proof band) and apply a composite mask-image to .portraitMedia so the outer edges fall off into the plane. Place the figure overlapping the mark's right end rather than beside it. Acceptance PH-1 (no border/outline/box-shadow on the media box or its children; zero portrait-tick nodes in the fold), PH-2 (mean |dL| across the rect boundary <= 0.04 on >= 3 of 4 edges, sampled 4px in vs 4px out), PH-3 (mask opaque over the upper-centre 46% — no gradient crosses the face), PH-4 (still a <figure> with a <figcaption>, figurePressables === 0), PH-5 (CLS 0 — a mask is compositing, no box resizes). Colour-agnostic by construction: nothing here samples inside the media rect, so the t_g2_h6 decision changes one filter declaration and nothing else.

## PROJECT ROOT
/root/forgotten-mistory (VPS srv1356245). Evidence: docs/delivery/evidence/v10-20260905T0515Z/. Live: https://forgotten-mistory.web.app. Static servers already bound by other tenants: :5599 and :8080 — never reuse them; council batteries use :5601 / :5602.

## MANDATORY
Call kanban_complete() when ALL gates pass — i.e. return structured output {task_id, gates:{...}, files_changed:[...], evidence:[...], goal_complete:true}; the orchestrator writes it to the board. Never self-approve; never weaken a check to pass it; every claim cites a command or a path. No secrets in output — read keys by name from /root/.claude/.env.production with a `grep -E '^[A-Z][A-Z0-9_]*='` reader, never `source` it, never print values.

## EXECUTION ORDER
- Read HeroPortrait.tsx and the .portrait* rules in Hero.module.css; note which marks are fold-only and which the proof band still needs
- Extend tests/e2e/hero-photo.spec.ts with PH-1..PH-5; capture red
- Remove the frame/ticks/cross from the fold render path and delete their now-dead CSS in the same commit (TC-NFR-DEADCSS fails the audit otherwise)
- Apply the composite mask to .portraitMedia and place the figure against the mark
- Verify the reduced-motion and no-JS paths still show the still at full opacity — the mask must not depend on JavaScript
- Screenshot 1440/1280/834/390 on both paths and look at the edge falloff against the pool

## QUALITY GATES
- Dead CSS removed in the same commit as the elements — TC-NFR-DEADCSS
- figurePressables === 0 — the figure never becomes a second call to action
- The <picture> still server-renders at full opacity and stays the LCP candidate at 390
- PH-3: the mask never crosses the face
- CLS 0 across 9 loads

## VERIFICATION
```bash
npx tsc --noEmit && npm run lint
npm run build:static && node scripts/validate/overhaul_static_audit.mjs
python3 -m http.server 5638 --directory out & sleep 3
PLAYWRIGHT_BASE_URL=http://127.0.0.1:5638 npx playwright test tests/e2e/hero-photo.spec.ts tests/e2e/hero-fold.spec.ts tests/a11y
node scripts/validate/hero_plane_dominance.mjs --base http://127.0.0.1:5638
```

## HIERARCHY
role_matrix: coding → level 2 → effort **xhigh** (effort_cascade.yaml; depth_cap 4). Model: claude-opus · Max OAuth. max_runtime_seconds 1800 (O1) · goal_max_turns 20.

## PROVIDER
Anthropic via OAuth (CLAUDE_CODE_OAUTH_TOKEN / claude-cli Max session). Never ANTHROPIC_API_KEY.

## COMMENT (2026-09-05T17:39:33.737Z)
1556Z: dispatch after t_h2_03 lands on live; photo into plane.

## COMMENT (2026-09-05T17:53:49.778Z)
C16 dispatch identity ap-gh1-04-c16. Continue /root/wt-gh1-04-1556 (Hero.module.css HeroPortrait.tsx hero-photo.spec.ts dirty). Rebase onto origin/main 58d9c111. Colour photo stays (G-H6). MiniVic pill must stay visible. Ledger before commit. Skip local Playwright. Push unique branch.

## STATUS (2026-09-05T17:53:50.323Z)
running — C16 AP ap-gh1-04-c16 photo-into-plane

## COMMENT (2026-09-05T17:58:40.522Z)
SHIPPED origin/worktree-gh1-04-1556 c917af04 (identity 1166df98): portrait frame/ticks removed, mask-image into plane. Duplicate AP ap-gh1-04-c16 CANCELLED. Do not complete until independent live reviewer on the deploy that contains this sha. Colour photo + MiniVic pill must still hold.

## COMMENT (2026-09-05T18:01:09.391Z)
Now on origin/main via consolidate ec03516; live build-commit 545df77b. Awaiting independent reviewer rev-545df77b-c17.

## COMPLETE (2026-09-05T19:32:50.457Z)
PASS subsumed by G-H1 live 64404134 (photograph in plane, not two-column card). https://forgotten-mistory.web.app/ build-commit 64404134 · independent rev-64404134-c22 · docs/delivery/evidence/v10-20260905T0515Z/G-REV/64404134/08-adversarial-review.md + verdicts.json
