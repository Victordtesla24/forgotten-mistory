# t_h2_02 — M1 — the light reaches the left: retire the desktop half-frame scrim for per-run plates

**Status:** todo · **Priority:** 96 · **Parents:** — · **Created:** 2026-09-05T15:19:23.349Z

## YOUR ROLE
analyst-programmer — coding (docs/prompt.md §5). [HERO-TASKS.json g2h1-02-retire-halfframe-scrim] HERO-FOLD-v2 §2 M1 / decision D-1. Hero.module.css .stage::after paints rgb(10 10 10 / 0.88) from 0% to 44% and 0.86->0.45 out to 56% — the half of the frame the eye enters through is switched off, which is the whole of 'cinematic stage reads as wallpaper'. Delete that wash above 700px and buy the contrast locally instead: promote the phone instrument (@media (max-width: 700px): background rgb(10 10 10 / 0.90), border-radius 3px, padding-inline .45rem, margin-inline -.45rem on .eyebrow/.name/.role/.statement plus the secondary action) to every width, sized to each run's own box and not a pixel wider. The 0.90 alpha is measured, not chosen: over this shader's brightest fog (0.8308 relative luminance) it lands the ground on #212121, inside the #2A2A2A ceiling --mist-400 needs for 4.5:1. Do NOT remove the <=700px rule — this extends it upward. VISIBLE: the atmosphere appears behind the copy at 1440/1280 where today it is 88% black. This is the first visible slice of the lane.

## PROJECT ROOT
/root/forgotten-mistory (VPS srv1356245). Evidence: docs/delivery/evidence/v10-20260905T0515Z/. Live: https://forgotten-mistory.web.app. Static servers already bound by other tenants: :5599 and :8080 — never reuse them; council batteries use :5601 / :5602.

## MANDATORY
Call kanban_complete() when ALL gates pass — i.e. return structured output {task_id, gates:{...}, files_changed:[...], evidence:[...], goal_complete:true}; the orchestrator writes it to the board. Never self-approve; never weaken a check to pass it; every claim cites a command or a path. No secrets in output — read keys by name from /root/.claude/.env.production with a `grep -E '^[A-Z][A-Z0-9_]*='` reader, never `source` it, never print values.

## EXECUTION ORDER
- Read Hero.module.css .stage, .stage::after and the @media (max-width: 700px) plate block end to end, including the notes that record why each number is what it is
- Delete .stage::after's wash above 700px; keep the element only if a slice still needs it, otherwise remove the rule and its display:none override together
- Give .eyebrow/.name/.role/.statement/.actions their own plate at every width, each sized to its own box
- Re-run the AA walk over #hero on ?gl=force at 1440 and 390: worst of ten must stay >= 4.5:1 (today 6.20:1)
- Re-run TC-HERO-SCRIM-01 (4 contexts) and the flagship floors; capture 04-tests-passing.log
- Screenshot the fold at 1440, 1280, 834, 390 into the evidence directory and look at every one

## QUALITY GATES
- Worst-of-ten AA over #hero >= 4.5:1 on ?gl=force AND on the reduced-motion still, at 1440 and 390
- TC-HERO-SCRIM-01 >= 0.06 in all four contexts
- Flagship floors hold: coverage >= 0.15, peak >= 0.35, motion >= 0.004, still coverage >= 0.08
- The <=700px plate rule is preserved, not replaced
- CLS 0 — a plate is a background on an existing box and must not change any box's size
- No plate alpha below 0.90 without re-measuring the ten worst nodes and recording the numbers

## VERIFICATION
```bash
npx tsc --noEmit && npm run lint
npm run build:static && node scripts/validate/overhaul_static_audit.mjs
python3 -m http.server 5636 --directory out & sleep 3
PLAYWRIGHT_BASE_URL=http://127.0.0.1:5636 npx playwright test tests/a11y/text-contrast.spec.ts tests/overhaul/flagship-visibility.spec.ts tests/e2e/hero-fold.spec.ts
node scripts/validate/hero_plane_dominance.mjs --base http://127.0.0.1:5636
```

## HIERARCHY
role_matrix: coding → level 2 → effort **xhigh** (effort_cascade.yaml; depth_cap 4). Model: claude-opus · Max OAuth. max_runtime_seconds 1800 (O1) · goal_max_turns 20.

## PROVIDER
Anthropic via OAuth (CLAUDE_CODE_OAUTH_TOKEN / claude-cli Max session). Never ANTHROPIC_API_KEY.

## STATUS (2026-09-05T15:20:06.161Z)
running — 15:19Z dispatched — analyst-programmer xhigh, isolated worktree, first visible hero slice (desktop half-frame scrim retired for per-run plates)

## COMMENT (2026-09-05T16:57:53.109Z)
1556Z: scrim retirement shipped on b2ac21be; live still two-column résumé. Lane complete as M1 only; G-H1 continues on t_h2_03/04.

## COMPLETE (2026-09-05T16:57:53.160Z)
M1 scrim retirement live on b2ac21be; G-H1 fold still FAIL
