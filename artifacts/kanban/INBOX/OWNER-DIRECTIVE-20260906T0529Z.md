# OWNER DIRECTIVE — 2026-09-06T05:29Z (live build-commit 199f116c)

## Verbatim
> remove all this shabby UI/UX, visualisations etc., and replace it with MARVEL STUDIOS grade visual effects and visualisations quality and sophestication. Only black, white..

Attached by the Owner: four screenshots of the live site (hero, About, Experience, Vitrine) — copied unaltered to
`docs/delivery/evidence/v10-20260905T0515Z/OWNER-20260906T0529Z/0{1,2,3,4}-*.png` (SHA256SUMS beside them).
These frames are the FAIL baseline for the whole visual layer: the Owner grades every current visualisation
(hero atmosphere + plates, About compass/field, Experience strata/descent, Skills bench, Vitrine drawings/field,
Listen field) as below the bar. They are to be **replaced, not polished**.

## Reading (docs/prompt.md §0.1 — the Owner is not asked)
1. "MARVEL STUDIOS grade visual effects and visualisations" is the new bar for R1/R2/§0.3-1: cinematic
   title-sequence craft — light as a material, volumetric depth, kinetic typography, camera motion, film
   grain/halation, no flat fills — turned into **measurable gates** by the solutions-architect (R7: no adjectives,
   instruments only). The site never claims or uses Marvel/Disney IP; named sequences are study material for craft.
2. "Only black, white.." — every effect is black, white and grey. **Assumption logged:** gold (`--gold`) remains
   exclusively the sourced-claim mark (docs/prompt.md §0.3-2, CLAUDE.md prime directive 4) and never appears in any
   effect. If the Owner meant no gold at all, the change is one token and one test; the assumption is reversible.
3. Everything else in docs/prompt.md stays binding: copy is the content files' copy (R7), LCP < 2.5 s, CLS < 0.05,
   asset budgets, nothing plays by default, reduced-motion + no-GL paths fully readable, keyboard, contrast, G-MV1,
   six sections in order, tests superseded consciously and replaced (never deleted), O1/O5 visible ship ≤ 10 min.

## Disposition of in-flight work
- Functional P0 regressions from t_w1_rev7 (390 dock swallows the CV tap; MiniVic panel placement 1440/1366) stay
  P0 — independent of visuals → t_w3_p0a.
- Hero v3.1 SA re-brief (translucent plate, SET-04/05) — **superseded**; its findings become constraints in the new
  direction (the name written across the light, nothing occluding the plane).
- t_w2_x2t3 / t_w2_x2f8 (About field tuning at 1440) — **stopped, superseded**; the About story metric requirement
  (ABOUT-STORY-v2 §metric) carries into the new spec as a gate on whatever instrument replaces the compass.
- Scene-7 polish (t2/f0/f1/s3), R3 avatar lanes, R2 fps proof, R5 4K — unchanged in requirement; re-sequenced after
  the direction is chosen so they are built once, on the new visual layer.

## Plan (wave 3)
W3-1 judge panel: two solutions-architects design independently (A "title-sequence school", B "simulation school"),
each with a standalone hero prototype for real frames → W3-2 reviewer judges on gates + rubric + feasibility, SA
synthesises CINEMATIC-VFX-v1 + slices → W3-3 analyst-programmer slices ship hero-first, each ≤ 30 min and visible on
the live URL → reviewer on every deploy (O2/O6) → PEA loop until PASS.
