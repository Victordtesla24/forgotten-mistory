# RECTIFY NOW — Orchestrator direction correction

**Issued:** 2026-09-05T12:52Z by host compliance audit of terminal 19  
**You are mid-run.** Do not restart the mission. Apply this correction immediately. Non-interactive.

## Binding reminder

`docs/prompt.md` + `docs/ORCHESTRATOR-LAUNCH-PROMPT.md` still govern. Compliance audit: `docs/adversarial/ORCH-COMPLIANCE-20260905T1245Z.md` — **O5 / first-ship FAIL**; later recovery partial.

## Hard corrections (do these in order, starting now)

1. **Stop waiting on Playwright before push.** O3: suite is evidence, not a gate. If `build:static` works → commit → push branch → let Deploy consolidate. Run Playwright in parallel after the push.
2. **Within the next 5 minutes:** push any dirty WIP that advances G-H1 / open P0s (hero fold, Skills GL if ready). Do not hold for green batteries.
3. **Close board lag:** `kanban_complete` G-V1 (and G-V2 if scoped) — independent reviewer already PASS on live. Same for any gap with PASS evidence sitting open.
4. **Finish G-A1 correction** — only close when live evidence strings that are *checkable records* are gold; grey for non-sourced is OK. Re-probe with reviewer.
5. **Verify MiniVic live:** greeting MP3 text match + send path hits `/api/chat` only (no `/api/realtime` / `/api/chat-with-vic`). If not live yet, push/redeploy now.
6. **Hermes:** ensure `pgrep -af 'hermes gateway'` is empty (not just systemd inactive). Do not start Hermes.
7. **Cadence truth:** never claim O1/O5 met from Deploy success alone. Every ≤10m window needs a **recruiter-visible** delta OR an explicit “no P0 WIP ready” note with evidence. Docs-only Deploys fail O5 when P0 UI WIP exists.
8. **Spawn discipline:** if a Workflow slot fails to start, re-dispatch within **2 minutes**, not 20.

## Parallel wave (next 10 minutes)

| Lane | Action |
|------|--------|
| analyst-programmer | Ship G-H1 remainder if not on live; cut remaining CV-dump if fold still dense |
| analyst-programmer | G-S1 Skills GL flagship — smallest visible canvas |
| reviewer | Live re-probe after next Deploy: G-H1, G-M1, G-M2, G-A1c |
| cleanup-agent | Kill any leftover Hermes processes; confirm timer active |

## Forbidden

- Asking the Owner  
- `ANTHROPIC_API_KEY`  
- Hermes  
- Renaming gaps to Done without live reviewer PASS  
- Another 10+ minute scout block before the next push  

## Acknowledge

One board comment on `t_g_rev`: `RECTIFY 12:52Z applied — Playwright unblocked; WIP push; board closes on PASS.` Then execute.
