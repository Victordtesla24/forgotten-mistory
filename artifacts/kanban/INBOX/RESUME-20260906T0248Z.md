# RESUME — session_01WWt1D5i764agLExdpEWvRC (written 2026-09-06T02:48Z, updated by the PM each cycle)

If this session hits the Pro/Max limit, paste `docs/ORCHESTRATOR-MASTER-PROMPT.md`, read
`docs/prompt.md`, then read this file and `artifacts/kanban/board.json` — do NOT restart work.

## Live state (PM-verified)

- Live build-commit at 02:49Z: `97e19d07` = About field story, on-demand rungs, MiniVic correction, OG card,
  ladder doc, launcher click (mv2), hero plane S1 (h1s1), career-descent mount (x2s1). Served HTML carries
  seven `data-scene` mounts. Reviewer `t_w1_rev4` (rev-97e19d07-w1, workflow `wf_a604cf9b-3b3`) is
  running on it — read its `G-REV/97e19d07/verdicts.json` before dispatching corrections.
- Reviews on live: `G-REV/56ffed3e` (G-H6 PASS, G-C1 PASS, G-H5 FAIL→fixed), `G-REV/ec53e2b4` (G-R2
  PARTIAL, G-M4 FAIL→correction shipped), `G-REV/12cd9123` (G-A3 PASS on the gap, G-H5 PASS, G-MV1 FAIL
  at 390→fixed in mv2). Overall production verdict is still FAIL until G-H1 S2–S4, scene 7 s2/s3 and the
  R3 second-form slices land with PASS.
- Cycle reports: `artifacts/kanban/cycles/c26-*.json`, `c27-*.json`.

## Lanes that were running (claude-cli Workflow ids under
`~/.claude/projects/-root-forgotten-mistory/4e7be839-6751-4a3e-83ba-4a9687938f6a/subagents/workflows/`)

| Workflow | Tasks | State at 02:48Z |
|---|---|---|
| `wf_728a696b-1af` fm-wave2-hero | t_w2_h1s1 (pushed 9287089) → t_w2_h1s2 (running) → s3 → s4 | S2 building |
| `wf_09fe4d50-430` fm-wave2-scene7 | t_w2_x2s1 (pushed daa4f57) → t_w2_x2t1 → t_w2_x2s2 | t1 or s2 running |
| `wf_6b600577-347` fm-wave1-tester-red | t_w1_red2 (9deea50) → t_w1_red3 (a2891fc) | DONE, both live (4043b8e9, 0580bd24) |
| `wf_f5f09863-eaa` fm-wave2-corrections | t_w2_x2f5 → t_w1_mv3 → t_w2_r3a2 | x2f5 running (03:17Z) |
| `wf_a604cf9b-3b3` fm-wave1-review-97e19d07 | t_w1_rev4 | reviewer running on 97e19d07 |

Bookkeeping branches pushed: `bookkeeping/w1-board`, `w2-board`, `w2-board-2`, `w2-board-3` (the last
restores 32 ledger rows lost to a -X theirs consolidation and adds `.gitattributes merge=union`).

If a lane died: re-dispatch its board task with a FRESH identity (never resume), branch from `origin/main`,
check `git ls-remote --heads origin` first so pushed work is not redone (§0 continuity).

## Update 03:28Z

- Cycle `c28-20260906T0325Z.json` written. Live `0580bd24`; queued: `bookkeeping/w2-board-3`,
  `worktree-w2-h1s2` (hero S2), `worktree-w2-x2t1` (story tests). Review 4 (`G-REV/97e19d07`) → G-R2,
  G-MV1, G-OG1 PASS; G-M4 FAIL on the Hosting fallback only (→ `t_w1_m4b`, §14 decision on `t_w1_r2c`);
  TC-BOT-14 at 1440 (→ `t_w1_mv4`); doc residue (→ `t_w1_doc2`, running).
- Hero S2 honest reds: 390 SPD 0.7153, H1 contrast 1.10:1 on the font-box rect → S4 must resolve (comment
  on `t_w2_h1s4`); reviewer `t_w1_rev5` is staged for the SHA carrying S2 — dispatch it as soon as live moves.
- Running: hero S3 (`wf_728a696b-1af`), scene-7 s2 (`wf_09fe4d50-430`), corrections x2f5→mv3→r3a2
  (`wf_f5f09863-eaa`), coder doc2 (`wf_1851ae8c-a20`). Ready, waiting for a heavy slot: `t_w1_m4b`,
  `t_w1_mv4`, `t_w2_l1m`, then `t_w2_h1s5` after S3.

## Update 03:52Z

- Live `e90c4e43` (About light correction x2f5 on top of hero S2 3657baa1). Review 5 (`G-REV/3657baa1`):
  name readable on its plate, but GRADED FAIL F-1 — the plate is cut at the baseline so the 'p' descender
  of Deshpande hangs onto the bright plane at every viewport (routed to hero S3/S4 comments); SPD 390
  reduced-motion 0.7153 < 0.75; S2's "≥ 0.78 at 1280" registered as a false positive; TC-BOT-14 → `t_w1_mv4`;
  gold body text in #about → `t_w2_a3g`. Everything else in the regression table PASS.
- Ready, waiting for a heavy slot (≤ 3 heavy lanes): `t_w1_mv4` (panel over the name — most visible),
  `t_w1_m4b`, `t_w2_l1m`, `t_w2_a3g`, `t_w2_h1s5` (after S3).

## Update 04:09Z

- Live `1ba16f90` (About state-gate light 479e2b2 + scene-7 depth c29b55e1). The About lane then
  REVERTED its own shader change (ef2979a, consolidating) because dimming the open wedges broke the
  flagship coverage floor (15.52% → 13.66%) and recovering area broke 390 contrast — three-way tension
  handed to `t_w2_a3sa` (SA, running, writes docs/architecture/ABOUT-STORY-v2.md). Extended
  TC-SCENE-ABOUT-10 stays red-by-design in the repo.
- Running: hero S3 (`wf_728a696b-1af`), corrections A mv3→r3a2 (`wf_f5f09863-eaa`), corrections B
  mv4→m4b→l1m→a3g (`wf_b7d17874-a2c`), reviewer 6 on 1ba16f90 (`wf_2de41482-6c9`), SA about (`wf_a5bbb7dc-981`).
- Scene 7 s1/t1/s2 all landed (7 data-scene mounts served; spans recoverable from light r 0.994).

## Update 04:21Z

- Live `83f4b208` (hero S3 typography lockup on top of the About revert and scene-7 depth).
- About tension resolved by the SA (`docs/architecture/ABOUT-STORY-v2.md`, decision c): open sectors drawn as
  structure at unchanged luminance; story assertion → per-sector DFT mark metric (≥ 3× answered, ≥ 0.20)
  + non-inversion floor 1.20. Slices on the board: `t_w2_x2f6` (ready, AP, bounded tuning window, stop
  conditions) and `t_w2_x2f7` (conditional — fire only on a named trigger).
- Hero: S3 landed (ratios 5.60/5.12/3.34/2.89 in band); S4 running (must fix the descender plate F-1,
  390 SPD 0.7153, 1280 ≥ 0.78); `t_w2_h1s5` (tester, hero suites + TC-FOLD-03 baselines after S4) ready.
- Still running: corrections A (mv3 → r3a2), corrections B (mv4 → m4b → l1m → a3g), reviewer 6 on 1ba16f90.

## Update 04:31Z

- Cycle `c29-20260906T0430Z.json` written. Review 6 (`G-REV/1ba16f90`): regression table all PASS; hero
  descender plate STILL FAIL after S3 (→ S4, running); 390 reduced-motion SPD 0.7487; About story clause
  unfixed on live (revert) → `t_w2_x2f6` RUNNING (`wf_1885d8ff-202`) with the SA's per-sector metric;
  scene-7 duration→spacing instruments disagree (0.994 vs 0.214) → `t_w2_x2t3` (tester, ready);
  process failure registered on `t_w2_x2f5` (push on a truncated log).
- Lanes: hero S4 (`wf_728a696b-1af`), corrections A mv3→r3a2 (`wf_f5f09863-eaa`), corrections B
  mv4→m4b→l1m→a3g (`wf_b7d17874-a2c`), About mark x2-f6 (`wf_1885d8ff-202`). Next reviewer: on the SHA
  carrying S4 (descender plate graded) — create `t_w1_rev7` then.

## Update 04:50Z — COEXISTENCE RULE (read before any git command)

- A sibling claude session (pid 1927798, pts/1, started 2026-09-05 16:43, cwd /root/forgotten-mistory —
  the Owner's Cursor-side session) is editing the MAIN checkout's working tree on `main`, uncommitted
  (scene shaders, Hero.tsx title card, CSS modules). Do NOT run git checkout/reset/stash on app paths in
  /root/forgotten-mistory. PM bookkeeping now happens in `.claude/worktrees/pm-board` (detached at
  origin/main): copy artifacts/kanban/**, the unioned ledger, docs/architecture/*, small evidence into it,
  commit there, push `HEAD:refs/heads/bookkeeping/<n>`. Council lanes keep their own worktrees from origin/main.
- Live `afad076b` (mv3 contrast fix). Running: hero S4, corrections A (r3a2), corrections B (mv4), About mark x2-f6.
  Ready: `t_w2_x2t3`, `t_w1_mv5` (launcher ground regression), `t_w2_h1s5` (after S4).

## Update 04:53Z

- `t_w2_x2f6` stopped honestly (21ff176d: tests + constants only; the open-mark shader term is saved as
  `W2-A3/x2-f6/03-shader-E1-E3.patch`): at 1440 at rest the ring annulus is guarded, so the `(1−guarded)`
  gate zeroes the mark (ratio 0.56); at 390 the mark works (ratio 2.98, three sectors visibly ruled).
  `t_w2_x2f7` RUNNING (`wf_5e6a0ec6-54e`): measure the guard mask over the annulus, relocate guarded UI at
  both widths, apply the patch, full About battery — never weaken a ceiling.
- Lanes: hero S4 (`wf_728a696b-1af`), corrections A r3a2 (`wf_f5f09863-eaa`), corrections B mv4→m4b→l1m→a3g
  (`wf_b7d17874-a2c`), About unguard x2-f7. Ready when a slot frees: `t_w2_x2t3`, `t_w1_mv5`, `t_w2_h1s5` (after S4).

## Update 05:00Z

- Live `fda84067` = hero S1–S4 (descender plate via 0.3em plate spread, SPD 0.79–0.93, gate armed at 0.78,
  PERF/PAL specs), panel placement fix (both-axes contract, 18–19 px), About-mark tests (red-by-design),
  R3-A2 (greeting alignment born with its own audio). Reviewer `t_w1_rev7` (`wf_10280795-ffd`) grades G-H1
  and R1 on it; tester `t_w2_h1s5` (`wf_03c432dc-043`) realigns the hero suites and runs the full battery.
- Still running: corrections B (m4b → l1m → a3g, `wf_b7d17874-a2c`), About unguard x2-f7 (`wf_5e6a0ec6-54e`),
  corrections A finishing r3a2 (`wf_f5f09863-eaa`). Ready: `t_w2_x2t3`, `t_w1_mv5`.
- Bookkeeping now from `.claude/worktrees/pm-board` (last push `bookkeeping/w2-board-5`, ledger 637 rows).

## Update 05:06Z

- `t_w2_x2f7` falsified its trigger (ring annulus 88–95% unguarded) and shipped only the probe; x2-f6's
  "guard zeroes the mark" diagnosis is registered as false; the cause is inside the mark term/estimator →
  `t_w2_x2f8` (AP, instrument ruling/groove/floor in order, ≤ 3 builds).
- `t_w2_r3a2` landed (greeting alignment born with its audio; premade voice 'Charlie' because the configured
  id is a cloned voice the API refuses) → voice consistency folded into `t_w2_r3a4`. `t_w2_a3m` filed
  (About field unmounts for most of the section at 390).
- Running: reviewer 7 (`wf_10280795-ffd`), tester S5 + full battery (`wf_03c432dc-043`), corrections B
  m4b→l1m→a3g (`wf_b7d17874-a2c`), corrections C mv5→x2t3→x2f8 (`wf_a6929fe1-820`).

## Update 05:17Z

- Live `314d9d28` (docs/evidence on top of fda84067). `t_w1_m4b` pushed 71f637d (fallback-only cap 48
  tokens on the Hosting route, route + max_tokens on the wire, trimmed-to-sentence answers, disclosure
  variant; function redeployed) → consolidating; reviewer `t_w1_rev8` staged (strict-cold both routes,
  capped-answer honesty, 1366 composer geometry, OCCLUDE-02 if mv5 landed) — dispatch after rev7 returns.
- Running: rev7 (`wf_10280795-ffd`), tester S5 (`wf_03c432dc-043`), corrections B now l1m→a3g
  (`wf_b7d17874-a2c`), corrections C mv5→x2t3→x2f8 (`wf_a6929fe1-820`).

## Update 05:22Z — load-shed

- Live `87eb005a` carries the G-M4 fallback cap (route/max_tokens on the wire verified by PM probe).
- At 1.35 GB free / swap full the PM stopped corrections-B (`wqtbctr1m`): m4b is pushed and live (report
  lost, commit body + probe stand in); `t_w2_l1m` and `t_w2_a3g` re-queued as ready — dispatch only when
  avail RAM > 3 GB and ≤ 2 other heavy lanes run. Running: rev7, tester S5 (full battery), corrections C
  (mv5 → x2t3 → x2f8). Reviewer `t_w1_rev8` staged behind rev7.

## Next PM actions in order

1. When live carries mv2 + h1s1 + x2s1: dispatch reviewer `t_w1_rev4` (spec on the board, scope notes in
   its comments) — read-only, max effort, live URL only.
2. On S3 landing: dispatch tester `t_w2_h1s5` (hero suites realigned to v3), then S4.
3. Ready but undispatched (priority order): `t_w2_x2f5` (About answered/open light), `t_w2_r3a2`,
   `t_w2_r3a4` (R3 second form), `t_w2_x2t2`, `t_w2_x2f0`, `t_w2_x2f1`, `t_w2_x2s3`, `t_w2_r3a3/a5/a6/a7`.
   Keep ≤ 3 heavy lanes (build/Playwright/encode) on this 4-core host; encodes `nice -n 19 -threads 2`.
4. Cleanup-agent remainder (dirty worktrees archive-then-remove) — classifier blocked two briefs; keep
   deletion detail in the task file, not the prompt (`t_clean2315`).
5. Write cycle c28 with Deploy id + live SHA + reviewer-4 verdict; commit board as a `bookkeeping/*`
   branch (never push main directly).

## Facts that must not be rediscovered

- OpenRouter balance −5.384 USD → every paid rung 402; §0.4 happy path returns only with a top-up
  (min 5.39, recommended 25 USD). Live answers come from the labelled OpenAI last rung.
- Higgsfield: 0 credits, free plan, and NO real-time surface at any tier → R3 first form is structurally
  OPEN; second form (pre-rendered + timestamped lip-sync) is the R3-A2…A7 slices.
- 4K portrait master: `artifacts/masters/minivic-greeting-2160p-master.mp4` (untracked, never commit).
- `minivic-greeting.mp3` still speaks "his AI clone" — regenerated by `t_w2_r3a2`'s single TTS call.
- Hermes stays off. Never `ANTHROPIC_API_KEY`. Never ask the Owner.

## Update 05:47Z — OWNER DIRECTIVE (wave 3)

- At 05:29Z the Owner wrote (verbatim in `artifacts/kanban/INBOX/OWNER-DIRECTIVE-20260906T0529Z.md`, four live
  screenshots beside it under evidence `OWNER-20260906T0529Z/`): replace the whole visual layer with
  Marvel-Studios-grade VFX and visualisations, black and white only. Live `199f116c` (deploy 34014452824).
- Wave-3 plan and board: root `t_w3_own`; directions `t_w3_dA` (title-sequence) + `t_w3_dB` (simulation) running
  in `fm-wave3-directions` (task w9oeu56b8, run wf_c1e36f0d-032) → prototypes `t_w3_pa`/`t_w3_pb` (serialized,
  ports 5603/5604) → judge `t_w3_jdg` (reviewer, independent) → synthesis `t_w3_syn` → slices from
  `docs/architecture/CINEMATIC-VFX-TASKS.json`, hero first, ≤ 30 min each, visible on live.
- P0 functional lane `t_w3_p0a` (fm-wave3-p0a, task we0elz5e5, run wf_c60089b0-cba, port 5605, branch
  `worktree-w3-p0a`): 390 dock inert until painted; panel placement at 1440/1366/1280/834/390.
- Paused by the Owner's interrupt at 05:30Z: tester S5 (wbtp5553u) and corrections C (wn56e5bun). Do not resume
  them; S5's battery child in `/root/wt-w2-h1s5` finishes on its own (baseline log), corrections C is superseded.
- Superseded: hero v3.1 re-brief, `t_w2_x2t3`, `t_w2_x2f8`. Deferred until the direction lands: rev8 (after
  p0a deploys), l1m, a3g, a3m, r3a3–a7, scene-7 t2/f0/f1/s3.
- Assumption on record: gold remains the sourced-claim mark only and appears in no effect.
- Cycle `c30-20260906T0547Z.json` written. Load-shed: ≤ 2 heavy lanes (S5 battery counts until it exits).
