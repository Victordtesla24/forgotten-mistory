# 02-tests-failing.log — S-1 red, recorded before any fix
# HEAD 7ae763a6; out/ was built from the unmodified source so the out/sw.js assertions are real
$ node --test tests/hosting_cache.test.mjs tests/sw_strategy.test.mjs
TAP version 13
# Subtest: the HTML document revalidates on every load
    # Subtest: declares a Cache-Control for "/"
    not ok 1 - declares a Cache-Control for "/"
      ---
      duration_ms: 11.585564
      type: 'test'
      location: '/root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/hosting_cache.test.mjs:68:5'
      failureType: 'testCodeFailure'
      error: 'firebase.json has no headers entry with source "/"'
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: true
      operator: '=='
      stack: |-
        TestContext.<anonymous> (file:///root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/hosting_cache.test.mjs:70:14)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Test.start (node:internal/test_runner/test:944:17)
        node:internal/test_runner/test:1440:71
        node:internal/per_context/primordials:466:82
        new Promise (<anonymous>)
        new SafePromise (node:internal/per_context/primordials:435:3)
        node:internal/per_context/primordials:466:9
        Array.map (<anonymous>)
      ...
    # Subtest: declares a Cache-Control for "/index.html"
    not ok 2 - declares a Cache-Control for "/index.html"
      ---
      duration_ms: 0.250551
      type: 'test'
      location: '/root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/hosting_cache.test.mjs:68:5'
      failureType: 'testCodeFailure'
      error: 'firebase.json has no headers entry with source "/index.html"'
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: true
      operator: '=='
      stack: |-
        TestContext.<anonymous> (file:///root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/hosting_cache.test.mjs:70:14)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Promise.all (index 0)
        async Suite.run (node:internal/test_runner/test:1442:7)
        async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
      ...
    # Subtest: declares a Cache-Control for "**/*.html"
    not ok 3 - declares a Cache-Control for "**/*.html"
      ---
      duration_ms: 0.243162
      type: 'test'
      location: '/root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/hosting_cache.test.mjs:68:5'
      failureType: 'testCodeFailure'
      error: 'firebase.json has no headers entry with source "**/*.html"'
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: true
      operator: '=='
      stack: |-
        TestContext.<anonymous> (file:///root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/hosting_cache.test.mjs:70:14)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: declares a Cache-Control for "/404.html"
    not ok 4 - declares a Cache-Control for "/404.html"
      ---
      duration_ms: 0.136781
      type: 'test'
      location: '/root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/hosting_cache.test.mjs:68:5'
      failureType: 'testCodeFailure'
      error: 'firebase.json has no headers entry with source "/404.html"'
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: true
      operator: '=='
      stack: |-
        TestContext.<anonymous> (file:///root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/hosting_cache.test.mjs:70:14)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: revalidates rather than forbidding storage, so the ETag still saves the transfer
    not ok 5 - revalidates rather than forbidding storage, so the ETag still saves the transfer
      ---
      duration_ms: 0.696788
      type: 'test'
      location: '/root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/hosting_cache.test.mjs:75:3'
      failureType: 'testCodeFailure'
      error: |-
        expected exactly one Cache-Control for source "/", got 0
        
        0 !== 1
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: 1
      actual: 0
      operator: 'strictEqual'
      stack: |-
        oneCacheControlFor (file:///root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/hosting_cache.test.mjs:56:10)
        TestContext.<anonymous> (file:///root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/hosting_cache.test.mjs:77:21)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: never leaves an HTML source on Firebase's default one-hour browser cache
    not ok 6 - never leaves an HTML source on Firebase's default one-hour browser cache
      ---
      duration_ms: 0.297313
      type: 'test'
      location: '/root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/hosting_cache.test.mjs:88:3'
      failureType: 'testCodeFailure'
      error: |-
        expected exactly one Cache-Control for source "/", got 0
        
        0 !== 1
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: 1
      actual: 0
      operator: 'strictEqual'
      stack: |-
        oneCacheControlFor (file:///root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/hosting_cache.test.mjs:56:10)
        TestContext.<anonymous> (file:///root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/hosting_cache.test.mjs:91:38)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    1..6
not ok 1 - the HTML document revalidates on every load
  ---
  duration_ms: 15.101812
  type: 'suite'
  location: '/root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/hosting_cache.test.mjs:66:1'
  failureType: 'subtestsFailed'
  error: '6 subtests failed'
  code: 'ERR_TEST_FAILURE'
  ...
# Subtest: the rules that were already right stay right
    # Subtest: keeps /_next/static/** immutable — the hashed chunks are safe to hold forever
    ok 1 - keeps /_next/static/** immutable — the hashed chunks are safe to hold forever
      ---
      duration_ms: 0.434045
      type: 'test'
      ...
    # Subtest: keeps /sw.js on no-cache — a cached worker can never announce its own replacement
    ok 2 - keeps /sw.js on no-cache — a cached worker can never announce its own replacement
      ---
      duration_ms: 0.324643
      type: 'test'
      ...
    # Subtest: does not make an asset or a chunk revalidate — only the document does
    ok 3 - does not make an asset or a chunk revalidate — only the document does
      ---
      duration_ms: 0.305213
      type: 'test'
      ...
    # Subtest: leaves the "**" security block free of Cache-Control, so nothing is overridden by accident
    ok 4 - leaves the "**" security block free of Cache-Control, so nothing is overridden by accident
      ---
      duration_ms: 0.86807
      type: 'test'
      ...
    1..4
ok 2 - the rules that were already right stay right
  ---
  duration_ms: 2.365265
  type: 'suite'
  ...
# Subtest: navigations are network-first
    # Subtest: branches on the navigation request before consulting the cache
    not ok 1 - branches on the navigation request before consulting the cache
      ---
      duration_ms: 1.137592
      type: 'test'
      location: '/root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/sw_strategy.test.mjs:54:3'
      failureType: 'testCodeFailure'
      error: 'the navigation branch must be reached before the cache-first read, or navigations are still cache-first'
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: true
      actual: false
      operator: '=='
      stack: |-
        TestContext.<anonymous> (file:///root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/sw_strategy.test.mjs:61:12)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Test.start (node:internal/test_runner/test:944:17)
        node:internal/test_runner/test:1440:71
        node:internal/per_context/primordials:466:82
        new Promise (<anonymous>)
        new SafePromise (node:internal/per_context/primordials:435:3)
        node:internal/per_context/primordials:466:9
        Array.map (<anonymous>)
      ...
    # Subtest: fetches the document from the network and only falls back to the cache on failure
    not ok 2 - fetches the document from the network and only falls back to the cache on failure
      ---
      duration_ms: 0.409744
      type: 'test'
      location: '/root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/sw_strategy.test.mjs:67:3'
      failureType: 'testCodeFailure'
      error: 'the navigation branch must hit the network'
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: ''
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/sw_strategy.test.mjs:71:12)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Promise.all (index 0)
        async Suite.run (node:internal/test_runner/test:1442:7)
        async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
      ...
    # Subtest: keeps hashed sub-resources cache-first — they cannot go stale
    ok 3 - keeps hashed sub-resources cache-first — they cannot go stale
      ---
      duration_ms: 0.189812
      type: 'test'
      ...
    1..3
not ok 3 - navigations are network-first
  ---
  duration_ms: 2.960242
  type: 'suite'
  location: '/root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/sw_strategy.test.mjs:53:1'
  failureType: 'subtestsFailed'
  error: '2 subtests failed'
  code: 'ERR_TEST_FAILURE'
  ...
# Subtest: every deploy invalidates the precache
    # Subtest: carries a build-stamp placeholder in the source, not a hand-bumped literal
    not ok 1 - carries a build-stamp placeholder in the source, not a hand-bumped literal
      ---
      duration_ms: 0.286813
      type: 'test'
      location: '/root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/sw_strategy.test.mjs:90:3'
      failureType: 'testCodeFailure'
      error: 'public/sw.js must declare CACHE_VERSION as the __BUILD_STAMP__ placeholder'
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        /*
         * Service worker — offline-after-visit durability (SPEC NFR-DURABLE / §10 TC-NFR-DURABLE;
         * prompt §2 NN-2: the takeaway must survive disconnecting from the internet).
         *
         * This site is a Firebase static export (no app/api/*), so the worker is a plain static
         * file served from /sw.js with root scope. Strategy:
         *   • install  — precache the navigation shell ('/') and the CV dossier so the core
         *                takeaway is guaranteed offline on the very next load.
         *   • fetch    — cache-first for same-origin GET, caching each first-visit response. This
         *                replays the build-hashed Next.js chunks/CSS/fonts offline WITHOUT
         *                hard-coding any hashed filename (which changes every build). Navigations
         *                fall back to the cached shell when the network is unavailable.
         *   • activate — drop superseded versioned caches, then claim open clients.
         * skipWaiting + clients.claim let a freshly-registered worker control the current page so
         * the immediate next reload is served from cache.
         *
         * Cache-version bump (e.g. v1 → v2) on any precache/strategy change invalidates the old
         * cache via the activate cleanup below — that is the deploy-time refresh mechanism.
         */
        
        const CACHE_PREFIX = 'fm-static-';
        const CACHE_VERSION = 'v1';
        const CACHE_NAME = `${CACHE_PREFIX}${CACHE_VERSION}`;
        
        // Stable, non-hashed entry points only. Hashed assets are captured at runtime (below).
        const PRECACHE_URLS = ['/', '/docs/Vik_Resume_Final.pdf'];
        
        self.addEventListener('install', (event) => {
          event.waitUntil(
            (async () => {
              const cache = await caches.open(CACHE_NAME);
              await cache.addAll(PRECACHE_URLS);
              await self.skipWaiting();
            })(),
          );
        });
        
        self.addEventListener('activate', (event) => {
          event.waitUntil(
            (async () => {
              const keys = await caches.keys();
              await Promise.all(
                keys
                  .filter((key) => key.startsWith(CACHE_PREFIX) && key !== CACHE_NAME)
                  .map((key) => caches.delete(key)),
              );
              await self.clients.claim();
            })(),
          );
        });
        
        self.addEventListener('fetch', (event) => {
          const request = event.request;
        
          // Only same-origin GETs are cacheable. Let the browser handle the rest natively.
          if (request.method !== 'GET') return;
        
          let url;
          try {
            url = new URL(request.url);
          } catch {
            return;
          }
          if (url.origin !== self.location.origin) return;
        
          // Range requests (media streaming) must reach the network for 206 responses; caching a
          // partial response would corrupt playback. The hero/MiniVic video relies on this.
          if (request.headers.has('range')) return;
        
          // Next.js dev HMR is never cached (keeps `npm run dev` reloads live).
          if (url.pathname.startsWith('/_next/webpack-hmr')) return;
        
          event.respondWith(
            (async () => {
              const cache = await caches.open(CACHE_NAME);
              const cached = await cache.match(request);
              if (cached) return cached;
        
              try {
                const response = await fetch(request);
                // Cache successful, fully-formed same-origin responses for offline replay.
                if (response && response.status === 200 && response.type === 'basic') {
                  cache.put(request, response.clone());
                }
                return response;
              } catch (error) {
                // Offline: serve the cached navigation shell for navigations so the core
                // dossier still renders; otherwise surface the failure.
                if (request.mode === 'navigate') {
                  const shell = await cache.match('/');
                  if (shell) return shell;
                }
                const fallback = await cache.match(request, { ignoreSearch: true });
                if (fallback) return fallback;
                throw error;
              }
            })(),
          );
        });
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/sw_strategy.test.mjs:91:12)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Test.start (node:internal/test_runner/test:944:17)
        node:internal/test_runner/test:1440:71
        node:internal/per_context/primordials:466:82
        new Promise (<anonymous>)
        new SafePromise (node:internal/per_context/primordials:435:3)
        node:internal/per_context/primordials:466:9
        Array.map (<anonymous>)
      ...
    # Subtest: still deletes superseded caches on activate, and still claims open clients
    ok 2 - still deletes superseded caches on activate, and still claims open clients
      ---
      duration_ms: 0.139732
      type: 'test'
      ...
    # Subtest: stamps out/sw.js with the build commit, so the deployed cache name is new
    not ok 3 - stamps out/sw.js with the build commit, so the deployed cache name is new
      ---
      duration_ms: 0.313444
      type: 'test'
      location: '/root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/sw_strategy.test.mjs:108:3'
      failureType: 'testCodeFailure'
      error: "out/sw.js still ships the hand-bumped 'v1' cache name"
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: 'v1'
      actual: 'v1'
      operator: 'notStrictEqual'
      stack: |-
        TestContext.<anonymous> (file:///root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/sw_strategy.test.mjs:119:12)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: stamps the deployed worker with the commit HEAD points at
    not ok 4 - stamps the deployed worker with the commit HEAD points at
      ---
      duration_ms: 22.869046
      type: 'test'
      location: '/root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/sw_strategy.test.mjs:127:3'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /const CACHE_VERSION = '7ae763a6'/. Input:
        
        '/*\n' +
          ' * Service worker — offline-after-visit durability (SPEC NFR-DURABLE / §10 TC-NFR-DURABLE;\n' +
          ' * prompt §2 NN-2: the takeaway must survive disconnecting from the internet).\n' +
          ' *\n' +
          ' * This site is a Firebase static export (no app/api/*), so the worker is a plain static\n' +
          ' * file served from /sw.js with root scope. Strategy:\n' +
          " *   • install  — precache the navigation shell ('/') and the CV dossier so the core\n" +
          ' *                takeaway is guaranteed offline on the very next load.\n' +
          ' *   • fetch    — cache-first for same-origin GET, caching each first-visit response. This\n' +
          ' *                replays the build-hashed Next.js chunks/CSS/fonts offline WITHOUT\n' +
          ' *                hard-coding any hashed filename (which changes every build). Navigations\n' +
          ' *                fall back to the cached shell when the network is unavailable.\n' +
          ' *   • activate — drop superseded versioned caches, then claim open clients.\n' +
          ' * skipWaiting + clients.claim let a freshly-registered worker control the current page so\n' +
          ' * the immediate next reload is served from cache.\n' +
          ' *\n' +
          ' * Cache-version bump (e.g. v1 → v2) on any precache/strategy change invalidates the old\n' +
          ' * cache via the activate cleanup below — that is the deploy-time refresh mechanism.\n' +
          ' */\n' +
          '\n' +
          "const CACHE_PREFIX = 'fm-static-';\n" +
          "const CACHE_VERSION = 'v1';\n" +
          'const CACHE_NAME = `${CACHE_PREFIX}${CACHE_VERSION}`;\n' +
          '\n' +
          '// Stable, non-hashed entry points only. Hashed assets are captured at runtime (below).\n' +
          "const PRECACHE_URLS = ['/', '/docs/Vik_Resume_Final.pdf'];\n" +
          '\n' +
          "self.addEventListener('install', (event) => {\n" +
          '  event.waitUntil(\n' +
          '    (async () => {\n' +
          '      const cache = await caches.open(CACHE_NAME);\n' +
          '      await cache.addAll(PRECACHE_URLS);\n' +
          '      await self.skipWaiting();\n' +
          '    })(),\n' +
          '  );\n' +
          '});\n' +
          '\n' +
          "self.addEventListener('activate', (event) => {\n" +
          '  event.waitUntil(\n' +
          '    (async () => {\n' +
          '      const keys = await caches.keys();\n' +
          '      await Promise.all(\n' +
          '        keys\n' +
          '          .filter((key) => key.startsWith(CACHE_PREFIX) && key !== CACHE_NAME)\n' +
          '          .map((key) => caches.delete(key)),\n' +
          '      );\n' +
          '      await self.clients.claim();\n' +
          '    })(),\n' +
          '  );\n' +
          '});\n' +
          '\n' +
          "self.addEventListener('fetch', (event) => {\n" +
          '  const request = event.request;\n' +
          '\n' +
          '  // Only same-origin GETs are cacheable. Let the browser handle the rest natively.\n' +
          "  if (request.method !== 'GET') return;\n" +
          '\n' +
          '  let url;\n' +
          '  try {\n' +
          '    url = new URL(request.url);\n' +
          '  } catch {\n' +
          '    return;\n' +
          '  }\n' +
          '  if (url.origin !== self.location.origin) return;\n' +
          '\n' +
          '  // Range requests (media streaming) must reach the network for 206 responses; caching a\n' +
          '  // partial response would corrupt playback. The hero/MiniVic video relies on this.\n' +
          "  if (request.headers.has('range')) return;\n" +
          '\n' +
          '  // Next.js dev HMR is never cached (keeps `npm run dev` reloads live).\n' +
          "  if (url.pathname.startsWith('/_next/webpack-hmr')) return;\n" +
          '\n' +
          '  event.respondWith(\n' +
          '    (async () => {\n' +
          '      const cache = await caches.open(CACHE_NAME);\n' +
          '      const cached = await cache.match(request);\n' +
          '      if (cached) return cached;\n' +
          '\n' +
          '      try {\n' +
          '        const response = await fetch(request);\n' +
          '        // Cache successful, fully-formed same-origin responses for offline replay.\n' +
          "        if (response && response.status === 200 && response.type === 'basic') {\n" +
          '          cache.put(request, response.clone());\n' +
          '        }\n' +
          '        return response;\n' +
          '      } catch (error) {\n' +
          '        // Offline: serve the cached navigation shell for navigations so the core\n' +
          '        // dossier still renders; otherwise surface the failure.\n' +
          "        if (request.mode === 'navigate') {\n" +
          "          const shell = await cache.match('/');\n" +
          '          if (shell) return shell;\n' +
          '        }\n' +
          '        const fallback = await cache.match(request, { ignoreSearch: true });\n' +
          '        if (fallback) return fallback;\n' +
          '        throw error;\n' +
          '      }\n' +
          '    })(),\n' +
          '  );\n' +
          '});\n'
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        /*
         * Service worker — offline-after-visit durability (SPEC NFR-DURABLE / §10 TC-NFR-DURABLE;
         * prompt §2 NN-2: the takeaway must survive disconnecting from the internet).
         *
         * This site is a Firebase static export (no app/api/*), so the worker is a plain static
         * file served from /sw.js with root scope. Strategy:
         *   • install  — precache the navigation shell ('/') and the CV dossier so the core
         *                takeaway is guaranteed offline on the very next load.
         *   • fetch    — cache-first for same-origin GET, caching each first-visit response. This
         *                replays the build-hashed Next.js chunks/CSS/fonts offline WITHOUT
         *                hard-coding any hashed filename (which changes every build). Navigations
         *                fall back to the cached shell when the network is unavailable.
         *   • activate — drop superseded versioned caches, then claim open clients.
         * skipWaiting + clients.claim let a freshly-registered worker control the current page so
         * the immediate next reload is served from cache.
         *
         * Cache-version bump (e.g. v1 → v2) on any precache/strategy change invalidates the old
         * cache via the activate cleanup below — that is the deploy-time refresh mechanism.
         */
        
        const CACHE_PREFIX = 'fm-static-';
        const CACHE_VERSION = 'v1';
        const CACHE_NAME = `${CACHE_PREFIX}${CACHE_VERSION}`;
        
        // Stable, non-hashed entry points only. Hashed assets are captured at runtime (below).
        const PRECACHE_URLS = ['/', '/docs/Vik_Resume_Final.pdf'];
        
        self.addEventListener('install', (event) => {
          event.waitUntil(
            (async () => {
              const cache = await caches.open(CACHE_NAME);
              await cache.addAll(PRECACHE_URLS);
              await self.skipWaiting();
            })(),
          );
        });
        
        self.addEventListener('activate', (event) => {
          event.waitUntil(
            (async () => {
              const keys = await caches.keys();
              await Promise.all(
                keys
                  .filter((key) => key.startsWith(CACHE_PREFIX) && key !== CACHE_NAME)
                  .map((key) => caches.delete(key)),
              );
              await self.clients.claim();
            })(),
          );
        });
        
        self.addEventListener('fetch', (event) => {
          const request = event.request;
        
          // Only same-origin GETs are cacheable. Let the browser handle the rest natively.
          if (request.method !== 'GET') return;
        
          let url;
          try {
            url = new URL(request.url);
          } catch {
            return;
          }
          if (url.origin !== self.location.origin) return;
        
          // Range requests (media streaming) must reach the network for 206 responses; caching a
          // partial response would corrupt playback. The hero/MiniVic video relies on this.
          if (request.headers.has('range')) return;
        
          // Next.js dev HMR is never cached (keeps `npm run dev` reloads live).
          if (url.pathname.startsWith('/_next/webpack-hmr')) return;
        
          event.respondWith(
            (async () => {
              const cache = await caches.open(CACHE_NAME);
              const cached = await cache.match(request);
              if (cached) return cached;
        
              try {
                const response = await fetch(request);
                // Cache successful, fully-formed same-origin responses for offline replay.
                if (response && response.status === 200 && response.type === 'basic') {
                  cache.put(request, response.clone());
                }
                return response;
              } catch (error) {
                // Offline: serve the cached navigation shell for navigations so the core
                // dossier still renders; otherwise surface the failure.
                if (request.mode === 'navigate') {
                  const shell = await cache.match('/');
                  if (shell) return shell;
                }
                const fallback = await cache.match(request, { ignoreSearch: true });
                if (fallback) return fallback;
                throw error;
              }
            })(),
          );
        });
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/sw_strategy.test.mjs:138:12)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    1..4
not ok 4 - every deploy invalidates the precache
  ---
  duration_ms: 24.072169
  type: 'suite'
  location: '/root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/sw_strategy.test.mjs:89:1'
  failureType: 'subtestsFailed'
  error: '3 subtests failed'
  code: 'ERR_TEST_FAILURE'
  ...
# Subtest: an updated worker shows the new build without being asked
    # Subtest: reloads the page once on controllerchange
    not ok 1 - reloads the page once on controllerchange
      ---
      duration_ms: 0.310963
      type: 'test'
      location: '/root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/sw_strategy.test.mjs:143:3'
      failureType: 'testCodeFailure'
      error: 'the registrar must listen for controllerchange'
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        'use client';
        
        import { useEffect, useState } from 'react';
        
        type SwToast =
          | null
          | { kind: 'update'; reload: () => void }
          | { kind: 'dev'; version: string };
        
        /**
         * Registers the offline-durability service worker (public/sw.js) once the page has loaded
         * (SPEC NFR-DURABLE / §10 TC-NFR-DURABLE; prompt §2 NN-2). Registration is deferred to the
         * `load` event so it never competes with first-view rendering, and is feature-gated on
         * `'serviceWorker' in navigator`. It also surfaces three quiet, polite-live notifications:
         * an "update available" prompt with a
         * Reload action when a fresh worker is waiting, and (development only) the precache version
         * it tears down. The toast region is always in the DOM (an empty aria-live status) so the
         * notifications are announced in place without layout shift.
         */
        export default function ServiceWorkerRegister() {
          const [toast, setToast] = useState<SwToast>(null);
        
          useEffect(() => {
            if (typeof navigator === 'undefined' || !('serviceWorker' in navigator)) return;
        
            // The worker is a PRODUCTION-only durability feature. In development it must never run:
            // its cache-first strategy replays a stale precached shell against freshly re-hashed
            // `_next/static` chunks, surfacing as a 404 storm. So in dev we tear down any worker +
            // cache a previous visit left behind (and surface its version) and skip registration.
            if (process.env.NODE_ENV !== 'production') {
              navigator.serviceWorker
                .getRegistrations()
                .then((regs) => Promise.all(regs.map((r) => r.unregister())))
                .catch(() => undefined);
              if (typeof caches !== 'undefined') {
                caches
                  .keys()
                  .then((keys) => {
                    const stale = keys.filter((k) => k.startsWith('fm-static-'));
                    if (stale[0]) setToast({ kind: 'dev', version: stale[0] });
                    return Promise.all(stale.map((k) => caches.delete(k)));
                  })
                  .catch(() => undefined);
              }
              return;
            }
        
            const register = () => {
              navigator.serviceWorker
                .register('/sw.js')
                .then((reg) => {
                  // A new worker installing while one already controls the page ⇒ an update is
                  // available. A first install (no controller yet) is silent: the
                  // visitor did not ask about the cache.
                  reg.addEventListener('updatefound', () => {
                    const installing = reg.installing;
                    if (!installing) return;
                    installing.addEventListener('statechange', () => {
                      if (installing.state !== 'installed') return;
                      // Only the actionable case surfaces. A first install used to
                      // announce "Ready to work offline." over the hero's primary
                      // button on a 1280x720 screen — an implementation detail the
                      // visitor never asked about, covering the one thing they might
                      // click. An available update is different: it asks for a decision.
                      if (navigator.serviceWorker.controller) {
                        setToast({ kind: 'update', reload: () => window.location.reload() });
                      }
                    });
                  });
                })
                .catch(() => {
                  // Registration failure must never break the page; the site works without the
                  // worker (it only adds offline durability), so this is intentionally silent.
                });
            };
        
            if (document.readyState === 'complete') {
              register();
              return;
            }
            window.addEventListener('load', register, { once: true });
            return () => window.removeEventListener('load', register);
          }, []);
        
          return (
            <div className="sw-toast-region" data-sw-toast role="status" aria-live="polite">
              {toast?.kind === 'update' && (
                <span className="sw-toast">
                  A new version is available.
                  <button type="button" className="sw-toast__reload" onClick={toast.reload}>
                    Reload
                  </button>
                </span>
              )}
              {toast?.kind === 'dev' && <span className="sw-toast sw-toast--dev">cache · {toast.version}</span>}
            </div>
          );
        }
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/sw_strategy.test.mjs:144:12)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Test.start (node:internal/test_runner/test:944:17)
        node:internal/test_runner/test:1440:71
        node:internal/per_context/primordials:466:82
        new Promise (<anonymous>)
        new SafePromise (node:internal/per_context/primordials:435:3)
        node:internal/per_context/primordials:466:9
        Array.map (<anonymous>)
      ...
    # Subtest: guards the reload with a sessionStorage flag, so a failing worker cannot loop
    not ok 2 - guards the reload with a sessionStorage flag, so a failing worker cannot loop
      ---
      duration_ms: 0.218393
      type: 'test'
      location: '/root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/sw_strategy.test.mjs:148:3'
      failureType: 'testCodeFailure'
      error: 'an unguarded controllerchange reload can loop indefinitely'
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        'use client';
        
        import { useEffect, useState } from 'react';
        
        type SwToast =
          | null
          | { kind: 'update'; reload: () => void }
          | { kind: 'dev'; version: string };
        
        /**
         * Registers the offline-durability service worker (public/sw.js) once the page has loaded
         * (SPEC NFR-DURABLE / §10 TC-NFR-DURABLE; prompt §2 NN-2). Registration is deferred to the
         * `load` event so it never competes with first-view rendering, and is feature-gated on
         * `'serviceWorker' in navigator`. It also surfaces three quiet, polite-live notifications:
         * an "update available" prompt with a
         * Reload action when a fresh worker is waiting, and (development only) the precache version
         * it tears down. The toast region is always in the DOM (an empty aria-live status) so the
         * notifications are announced in place without layout shift.
         */
        export default function ServiceWorkerRegister() {
          const [toast, setToast] = useState<SwToast>(null);
        
          useEffect(() => {
            if (typeof navigator === 'undefined' || !('serviceWorker' in navigator)) return;
        
            // The worker is a PRODUCTION-only durability feature. In development it must never run:
            // its cache-first strategy replays a stale precached shell against freshly re-hashed
            // `_next/static` chunks, surfacing as a 404 storm. So in dev we tear down any worker +
            // cache a previous visit left behind (and surface its version) and skip registration.
            if (process.env.NODE_ENV !== 'production') {
              navigator.serviceWorker
                .getRegistrations()
                .then((regs) => Promise.all(regs.map((r) => r.unregister())))
                .catch(() => undefined);
              if (typeof caches !== 'undefined') {
                caches
                  .keys()
                  .then((keys) => {
                    const stale = keys.filter((k) => k.startsWith('fm-static-'));
                    if (stale[0]) setToast({ kind: 'dev', version: stale[0] });
                    return Promise.all(stale.map((k) => caches.delete(k)));
                  })
                  .catch(() => undefined);
              }
              return;
            }
        
            const register = () => {
              navigator.serviceWorker
                .register('/sw.js')
                .then((reg) => {
                  // A new worker installing while one already controls the page ⇒ an update is
                  // available. A first install (no controller yet) is silent: the
                  // visitor did not ask about the cache.
                  reg.addEventListener('updatefound', () => {
                    const installing = reg.installing;
                    if (!installing) return;
                    installing.addEventListener('statechange', () => {
                      if (installing.state !== 'installed') return;
                      // Only the actionable case surfaces. A first install used to
                      // announce "Ready to work offline." over the hero's primary
                      // button on a 1280x720 screen — an implementation detail the
                      // visitor never asked about, covering the one thing they might
                      // click. An available update is different: it asks for a decision.
                      if (navigator.serviceWorker.controller) {
                        setToast({ kind: 'update', reload: () => window.location.reload() });
                      }
                    });
                  });
                })
                .catch(() => {
                  // Registration failure must never break the page; the site works without the
                  // worker (it only adds offline durability), so this is intentionally silent.
                });
            };
        
            if (document.readyState === 'complete') {
              register();
              return;
            }
            window.addEventListener('load', register, { once: true });
            return () => window.removeEventListener('load', register);
          }, []);
        
          return (
            <div className="sw-toast-region" data-sw-toast role="status" aria-live="polite">
              {toast?.kind === 'update' && (
                <span className="sw-toast">
                  A new version is available.
                  <button type="button" className="sw-toast__reload" onClick={toast.reload}>
                    Reload
                  </button>
                </span>
              )}
              {toast?.kind === 'dev' && <span className="sw-toast sw-toast--dev">cache · {toast.version}</span>}
            </div>
          );
        }
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (file:///root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/sw_strategy.test.mjs:149:12)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Promise.all (index 0)
        async Suite.run (node:internal/test_runner/test:1442:7)
        async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: still offers the explicit Reload action for a worker that is merely waiting
    ok 3 - still offers the explicit Reload action for a worker that is merely waiting
      ---
      duration_ms: 6.604941
      type: 'test'
      ...
    1..3
not ok 5 - an updated worker shows the new build without being asked
  ---
  duration_ms: 7.405649
  type: 'suite'
  location: '/root/forgotten-mistory/.claude/worktrees/wf_b12ab287-6ab-1/tests/sw_strategy.test.mjs:142:1'
  failureType: 'subtestsFailed'
  error: '2 subtests failed'
  code: 'ERR_TEST_FAILURE'
  ...
1..5
# tests 20
# suites 5
# pass 7
# fail 13
# cancelled 0
# skipped 0
# todo 0
# duration_ms 303.231653
node --test exit=1
