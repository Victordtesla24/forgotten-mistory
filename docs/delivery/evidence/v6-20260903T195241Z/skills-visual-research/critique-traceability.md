STRONG / **PROMISING** / WEAK → **PROMISING**

**1. It does not teach "calibration card." It teaches "why not bars."**
Column 3 lands in under a second — a bar hanging off a dashed rule with two printed dashes is a genuinely good refutation. But nothing in the figure draws a *certificate*: no reference, no tolerance, no "not tested" region. The reader learns the negative claim and still cannot say what the section is.

Worse, the chain is upside down. Traceability descends *from* the highest standard; here the apex is `Vik_Resume_Final.pdf` — his own self-reported document, correctly graded grey — and `ANZ Banking Group` hangs **below** it. Drawn literally, that says ANZ derives from Vik's resume. The one metrology-literate reader this section is built to impress reads it as backwards; everyone else absorbs it wrong.

**2. Gold is misapplied.** The site's rule is *this figure has a source*. The gold jaws close on a proper noun, not a figure, while the actual reading (`10,000+ … P95 under 200 ms`) sits in plain white. And the sub-line concedes it: `verified by asking the employer` — a stranger cannot. The only genuinely stranger-checkable sources on this site are the repositories. The one gold rung is the least checkable specimen in the figure.

**3. Craft.** One grade-10 idea: the stile turning dashed *below* the break, so non-traceability propagates. Everything else is grade 6 — three columns × three rungs of 0.56–0.74rem mono across 78rem, type range 0.56→0.98rem (1.75×, no scale contrast), all 1px. Thin, not quiet. The Bench beside it has 13×17 wires; this will look like the Bench's caption.

**4. Collisions.** An apex fanning to three drops *is* the Bench's one-to-many grammar rotated 90°, forty pixels away. And branding the horizontal proportional bar as the discredited object retro-poisons Experience's sixteen honest to-scale bars one section up.

**5. Buildable, yes.** `--motion-` is genuinely in `APPROVED_PREFIXES` (audit ~line 397); DEADCSS is globals-only. Two misses: **TC-SKILL-08** requires the `Calibrated against … MD5` footer, so the "dead line" promoted to the apex must also stay — the fingerprint prints twice in one section. And Caliper `self-reported` appends **◐**, which the legend one screen below defines as *measured outside production*.

**Strongest fix: turn the ladders the right way up.** Put at the apex the thing a stranger can reach — the repository, opened now — and hang his claim *beneath* it; the CV becomes the instrument being calibrated at the bottom of the chain, not the standard at the top. Gold then falls on the reading whose source is genuinely reachable, the metaphor reads top-down as verification without a word of prose, and column 2's break and column 3's nothing become legible by contrast instead of by caption.