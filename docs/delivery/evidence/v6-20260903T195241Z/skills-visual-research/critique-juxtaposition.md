VERDICT: PROMISING

**1. Does it teach the term in 3 seconds? No.** It teaches "unsourced bars are bad" in 3 seconds — a different, easier lesson the lede already wins. The word *certificate* never appears in the drawing; the three compartment names that carry the entire pedagogical load are set at 0.52rem uppercase mono in `--mist-400` — smaller than anything currently in Skills (floor is 0.6rem) and the dimmest ink in the figure. The lesson is in the least legible type in the frame. A first-timer must read nine separate mono labels (two straps, three field names, NO UNIT · NO SOURCE, the seam stat, OUTSIDE THIS CERTIFICATE, CV 16b856c0) before the argument closes. That is a form, not a glance.

**2. Craft.** Grade 8. Real craft: the printed en-dashes, extension lines inset 3px, "gold lands last because that figure is sourced." But it claims "no new vocabulary" while introducing a GD&T feature-control frame — a fourth mark, exempted by sleight of hand because a Caliper sits inside it. And Band B is the table row `Real-time telemetry | 10,000+ concurrent devices… | ANZ | ● measured in production` re-typeset ~300px above the actual table row. Restating one row in fancier boxes is decoration.

**3. Rule violations — the fatal one.** `<Caliper state="sourced">` around the ANZ telemetry figure inflates a grade. `Hero.tsx:68` renders that same claim (`10k+`) as `state="self-reported"`; `sourced` is currently rendered *nowhere* on the site. An employer name is not a checkable artefact — that is the exact CT-10 pattern, and no Skills test guards it, so it ships silently. Also, the evidence string is retyped (`10,000+ devices · P95 < 200 ms` vs the data's `10,000+ concurrent devices held at P95 under 200 ms`) — hand-copied, not derived: fails "every visual is data."

**4. Collisions — two, both concrete.** `Bench.tsx:425` already prints `20 links · 13 sources · 17 capabilities`; the seam strap duplicates it one screenful above. And `Bench` is already `<figure>`+`<figcaption>`; this stacks a second captioned figure above it, then a captioned table — three captioned objects, header-first. Experience's `.trackBar` collision is worse than admitted: a bar labelled NO UNIT reads as a slur on Experience's honest axis.

**5. Buildable?** Yes, easily — and that's the risk. It renders as a competent form, not a revelation.

**STRONGEST FIX:** Delete Bands B and C. Keep only the retraction — the empty track, the axis, the printed dashes, `NO UNIT · NO SOURCE` — and let the *existing Bench and table* be the "as left." The certificate is already drawn twice below; stop drawing it a third time. Then spend the reclaimed 200px on one Source Serif line naming the object: **"A calibration certificate states what was tested, where, and what was not."**