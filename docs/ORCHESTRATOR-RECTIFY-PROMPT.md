# ORCHESTRATOR RECTIFY PROMPT — paste into the running ultracode session

You are still the ORCHESTRATOR under `docs/prompt.md` + `docs/ORCHESTRATOR-LAUNCH-PROMPT.md`. A host adherence scout found you are **PARTIAL**: good parallel §5 shipping, but **not exact** on clocks and board truth.

## Read immediately

1. `artifacts/kanban/INBOX/RECTIFY-20260905.md`
2. `/root/dir-mgmt/ORCHESTRATOR-ADHERENCE-20260905.md` (or `docs/adversarial/` if mirrored)

## Execute in order (no Owner questions)

1. **Reopen G-H2:** `t_g_h2` → `running` (not done). Comment: “Architecture docs are not PASS; awaiting live first-paint/poster PASS via t_x1_02/t_x1_03.”
2. **Hygiene:** move all ADV gap tasks with `status=done` to `column=done` (t_g_a1, t_g_a2, t_g_m1, t_g_m2, t_g_h1, t_g_v1, t_g_s1, etc.).
3. **Cap concurrency:** at most **2** Chrome/Playwright-heavy workflows at once while load average > 8; queue the rest.
4. **O5 rule restated:** the next Deploy must include a **visible** UI/CSS/GL change for an open P0 (prefer G-H2a), or you must file an O5-MISS comment with the empty window timestamps — then ship UI in the following tick.
5. **Split overruns:** any agent >25 minutes → checkpoint, push branch, new ≤30m child task.
6. **Reviewer cadence:** one live probe per Deploy for the gaps that changed; complete `t_g_rev` heartbeat tasks instead of infinite `running`.

## Do not

- Mark G-H2 / R1 / R2 / R3 PASS on docs alone  
- Ask the Owner  
- Start Hermes  
- Stall all P0s waiting for host load to clear without shedding Chrome load yourself  

Confirm intake on the board, then continue PEA.
